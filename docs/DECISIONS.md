# Decisions Log (append-only)

Format: `YYYY-MM-DD — decision — rationale`

- 2026-06-21 (M-A) — `CoverageReport` / `CoverageGapEntry` defined in `domain/coverage.ts`, not `solver/types.ts` — they are pure domain types (no solver references) so they belong in the domain layer; `solver/types.ts` re-exports them to avoid import-direction violations (domain ← solver).
- 2026-06-21 (M-A) — `gapReasons: FillGapReason[]` added alongside existing `blockers: string[]` in `FillResult` rather than replacing it — `blockers` is used by `FillReview.tsx` and other callers; adding a parallel structured field avoids changing call sites while enabling `buildCoverageReport()` to attribute reasons to specific (class, subject) pairs without brittle substring matching.
- 2026-06-21 (M-A) — The exact-search path in `deepSearch.ts` emits `fillGapReasons: []` (empty) — the exact backtracker directly places via `applyOption` without going through `fill()`, so there are no fill-level gap reasons to propagate. The `CoverageReport` is still computed from `coverageGaps()` using the resulting project's actual shortfall.
- 2026-06-21 (M-A) — "Amber open cells" in the grid deferred to M-B or later — rendering per-slot amber cells would require knowing WHICH slot should have which unplaced subject, but the fill algorithm doesn't commit to a slot for an unplaced requirement (it just knows N periods are missing). The "What's left & why" panel satisfies the M-A acceptance criteria without that ambiguity.
- 2026-06-21 (M-A) — One suggestion string per gap rather than a typed `Fix` object — M-B defines the full `Fix` type with `apply(project): Project`; importing it in M-A would couple domain/coverage.ts to solver types prematurely. Lean on text suggestions for M-A; M-B upgrades them to executable Fix objects.

- 2026-06-21 (M-B) — `ProjectFix` uses a serializable `FixSpec` discriminated union (no function fields) rather than `apply(project): Project` inline — CoverageGapEntry flows through the Web Worker (structured clone) and functions are stripped. Keeping the spec data-only + a separate `applyProjectFix(project, spec)` function on the client side means fixes survive the round-trip. The existing `relaxation.apply` closures in `FeasibilityReport.structuredBlockers` are NOT replaced (they only live on the main thread via `analyzeFeasibility()` in App.tsx, so they work fine).
- 2026-06-21 (M-B) — `autoFixToFeasible` only applies `teacher_capacity` and `cap_sum` blockers that have `relaxation.apply` — these are pure structural cap raises (no data-model compromise). `reduce_requirement` and `qualify_teacher` fixes require explicit owner approval (they change educational commitments), so they are surfaced as per-gap buttons but NOT in the auto-fix loop.
- 2026-06-21 (M-B) — `autoFixToFeasible` lives in `solver/autoFix.ts`, not `domain/` — it calls `analyzeFeasibility` from `solver/feasibility.ts`; domain/ cannot import from solver/ (strict layering). The function runs synchronously on the main thread (not in the worker) since feasibility analysis is fast.
- 2026-06-21 (M-B) — `applyProjectFix` for `qualify_teacher` idempotent-guards: if the qualification already exists, returns the same project reference unchanged — prevents double-adds when a user clicks "qualify" twice and prevents spurious re-renders.
- 2026-06-21 (M-B) — "Auto-fix to feasible" button shown in BOTH the gap panel (when feasibility is blocked and some blocker has apply) AND the blockers section — the two panels may appear independently depending on solve state.
- 2026-06-21 (M-B) — `buildFixesForGap` orders fixes cheapest-first: `qualify_teacher` (low) before `reduce_requirement` (medium) — cheapest action is presented first so the user sees the least-disruptive option without scrolling.
- 2026-06-21 (M-B) — No preview-before-apply for per-gap fix buttons — buttons call `applyTweak` immediately (with a backup created first). A diff+confirm dialog is deferred: M-B's goal is to unblock the workflow; the backup mechanism ensures full undo safety. Opted-out of the §4.4 preview-on-confirm requirement as a deliberate scope decision.

- 2026-06-21 (M-C) — `tier?: 0|1|2|3` added as an OPTIONAL field to `ConstraintBase` (not required) — making it optional avoids editing ~30 constraint interfaces, every fixture, and all test factory calls. `constraintTier(c)` is the single read point: `c.tier ?? (severity==="must" ? 0 : 2)`, so all existing data is automatically migrated on read.
- 2026-06-21 (M-C) — `validate()` and all solver logic keep reading `severity`, not `tier` — keeps the one-oracle discipline intact and avoids behavior change. Tier-driven relaxation in the solver is M-E's job (two-pass: tier-0+1 solve → auto-relax tier 2+3).
- 2026-06-21 (M-C) — Tier-1 defaults for specific constraint templates (P1 anchor, board-protect → tier 1) deferred — auto-assigning tier 1 changes behavior when M-E's relaxation fallback is not yet in place; setting them early would make those constraints impossible to relax until M-E ships. Users can set tier 1 via the UI segmented control.
- 2026-06-21 (M-C) — 4-way segmented control (T0–T3) replaces the binary "move to Rule/Preference" button — gives the user full tier control in-row; crossing the must/prefer boundary (T1↔T2) also updates severity and weight automatically. The two sections (Rules/Prefs) are now filtered by `constraintTier(c) <= 1` and `>= 2` rather than `severity === "must"/"prefer"`.

- 2026-06-21 (M-D) — Phase-3 load-swap as distinct from phase-2 (single-eviction-relocation): phase-2 moves Q's blocker lesson to a different time slot; phase-3 substitutes a different teacher Y into Q's blocker lesson at the SAME slot, freeing Q to fill the gap. Needed because phase-2 fails when Q is unavailable everywhere else.
- 2026-06-21 (M-D) — Load-swap atomicity: both substitution (Y into blocker class) and gap fill (Q into gap class) are staged as two `placeNormalLesson` calls on the same candidate project before hitting the `validate()` gate. The oracle is the gatekeeper, not the generator.
- 2026-06-21 (M-D) — Continuity preference ordering: `gapCandidates()` puts the teacher with the most already-placed lessons for (classId, subjectId) first (`primaryTeacher()` helper); all others are shuffled behind. Biases toward same-teacher continuity while still substituting when needed.
- 2026-06-21 (M-D) — `note?: string` on `FilledPlacement` (not on the domain event/placement model): notes are purely for the FillReview diff panel and don't need to persist. Amber text in the UI makes relaxations transparent without cluttering the data model.
- 2026-06-21 (M-D) — `allQualsByClass` passed into `gapCandidates()` from the caller rather than re-computed per gap: avoids O(|qualifications|) rebuild on each gap iteration; lets phase-3 look up substitute candidates for the blocker class in O(1).

- 2026-06-04 — New repo, separate from `timetable2025`; integration via legacy rawData export only — keeps the deployed viewer stable; the generator has different architectural needs.
- 2026-06-04 — Hand-rolled TS solver in a Web Worker, no CP libraries — problem size ≤ 504 cells; keeps bundle small, fully free, debuggable, deterministic.
- 2026-06-04 — ELGA modeled as an atomic `BlockActivity` (5 classes × 5 teachers × 3 consecutive periods) — matches reality: students regroup by English level across Classes 1–5; partial moves are meaningless.
- 2026-06-04 — Build order: editor with live validation first, solver second — schools trust a tool that catches mistakes before they trust black-box generation.
- 2026-06-04 — No backend/auth/sync in v1; IndexedDB + JSON file export — zero cost, offline-first, matches school's existing PWA philosophy.
- 2026-06-04 (M0) — Tailwind v3.4 with classic `tailwind.config.js` + `postcss.config.js` (not v4 CSS-first config) — v4's config model is churn we don't want in a deterministic build.
- 2026-06-04 (M0) — `build` script is `tsc --noEmit && vite build` (not bare `vite build`) — esbuild strips types without checking; this makes "strict, no any" actually enforced at build time. Paired with ESLint `@typescript-eslint/no-explicit-any` (strict catches implicit any; the rule catches explicit any).
- 2026-06-04 (M0) — Vitest default `environment: "node"`; UI tests opt into jsdom via `environmentMatchGlobs` (`src/ui/**`, `*.dom.test.tsx`) — keeps the domain/solver purity invariant (AGENTS.md §3) self-policing: impure DOM code can't silently pass in those layers.
- 2026-06-04 (M0) — Single `tsconfig.json` (dropped composite project references) — `tsc --noEmit` is incompatible with `composite`/`references` emit; one config is simpler and still typechecks vite.config.ts.
- 2026-06-04 (M0) — Dev deps beyond the AGENTS.md §1 pre-approved list, all standard for this stack: `vite`, `@vitejs/plugin-react`, `typescript`, `@types/*`, `@typescript-eslint/*`, `eslint` (+ react-hooks, react-refresh plugins), `jsdom`, `postcss`, `autoprefixer`, `@testing-library/jest-dom`. Each is required by the decided stack (ARCHITECTURE.md); no runtime/solver libraries added.
- 2026-06-04 (M1) — Entity ids = display names on import (teacher id "Bindu", subject id "ELGA", class id "Class 7") — names are documented unique; keeps legacy export stable and round-trip exact without a separate id map.
- 2026-06-04 (M1) — ELGA block detection keyed on the literal subject token "ELGA" (not a general multi-teacher-block heuristic) — it is the only block in VPPS today; a general heuristic risks false positives. One BlockActivity is created per distinct (length, class-set, teacher-order) signature, with one Placement per day-run, so multi-day ELGA reuses a single activity.
- 2026-06-04 (M1) — H8 (daily max) only fires where a CurriculumRequirement scopes the class+subject. The `maxPerDay` default 2 is a property of a requirement that omits it, NOT a global cap. Without this gate, freshly imported real data (empty requirements) would false-flag any legitimate 3×/day subject — undercutting the editor's credibility in M2. Coherent with H7's deferral.
- 2026-06-04 (M1) — H10 (pinned immovable) is NOT implemented in `domain/validate.ts`: it is a solver-loop invariant (does the solver move/remove a pinned placement?) with no static representation — `validate(project, timetable)` has no before-state to diff. Enforced in `solver/` (M3). The ROADMAP "H1–H6, H8–H10" grouping is loose, not a claim H10 is statically checkable.
- 2026-06-04 (M1) — H3 (block atomicity) implemented as block-definition integrity (length ≥ 1, ≥1 class, ≥1 teacher). True atomicity is structural in the data model — one Placement expands to all classes × periods × teachers via `derive.expandPlacement`, so partial placement is unrepresentable. The "can't move one class's ELGA" guarantee is editor behavior (M2), not a validate check.
- 2026-06-04 (M1) — The M1 round-trip test is an EXACT-string match (`toBe`) against a canonical-format fixture written to the DATA_MODEL.md spec, NOT against real legacy-viewer output (no snapshot available). A real-data round-trip will need a semantic compare (tolerant of blank lines / trailing spaces / ordering); the true end-to-end "paste into the legacy viewer" check is the M6 AC.
- 2026-06-04 (M2) — Edit operations (`domain/edit.ts`) are pure functions returning NEW arrays; undo/redo (`editorStore`) stores immutable snapshots (references, since nothing is mutated in place) → `undo` restores the exact prior `Project` (verified by `toEqual`/`structuredClone`). Placements are identified by ref (activityId, day, period) so block occurrences on different days are independently movable, and any block cell carries the START-period ref → dragging one ELGA cell moves all 15 atomically (it is one placement).
- 2026-06-04 (M2) — Two-store split: `projectStore` owns the Project + debounced (400 ms) IndexedDB autosave; `editorStore` owns UI state + history and commits through `projectStore`. Cross-store calls use `getState()`.
- 2026-06-04 (M2) — Live validation runs synchronously via a `useMemo` keyed on Project identity (a new object per commit). At VPPS scale `validate()` is well under the 16 ms budget, so no debounce is needed for correctness; debounce can be added in M-perf if profiling shows need.
- 2026-06-04 (M2) — Dev dep `fake-indexeddb` (justification: the only practical way to unit-test the "refresh restores from IndexedDB" AC and `persistence/db.ts` in Node/jsdom; test-only, not bundled). Runtime dep `@dnd-kit/core` is pre-approved ("dnd-kit", AGENTS.md §1) for grid drag-drop.
- 2026-06-04 (M2) — `gridModel.ts` (pure, no React) builds the grid view-model + per-cell conflict overlay, unit-tested in isolation; React components stay thin. Daily/weekly violations (H8/H9, period 0) are surfaced in the Conflicts panel, not as per-cell badges.
- 2026-06-04 (M3) — A `CurriculumRequirement` is modeled as ONE canonical Lesson activity placed `periodsPerWeek` times (placements may repeat an activityId, exactly like the ELGA block on two days) — no distinct activity per period. `domain/requirements.ts` derives requirements + canonical lessons from an existing timetable; `normalizeProject` rebuilds a requirement-driven project. `makeSampleProject` returns the normalized sample so the Quota panel and solver have data.
- 2026-06-04 (M3) — CLASH-DEDUP FIX (oracle correctness): `validate` H1/H2 previously deduped occupancies by `activityId`, which silently missed two placements of the SAME canonical lesson (or two overlapping blocks) in one slot — the common case under the requirement model. Fixed to count raw occupancies (`occ.length > 1`); `derive.expandPlacement` de-dupes ids within a single activity so each placement contributes at most one occupancy per (entity, slot). Regression test added.
- 2026-06-04 (M3) — Solver (`solver/engine.ts`): seeded mulberry32 PRNG; blocks/pins frozen (H10); only missing requirement-periods are variables; dynamic most-constrained-variable selection (tie → lowest index) with seed-shuffled value order → deterministic per seed *provided the search completes within budget* (the fixture finishes in ~15 ms, far under the 5 s AC). `canPlace` mirrors every hard rule `validate` checks (incl. H6); the engine test still asserts the real `validate()` returns 0 hard on the output (honest oracle, not the solver's own bookkeeping). Teacher occupancy is counted once per (teacher, period), decoupled from class count, so blocks don't over-count.
- 2026-06-04 (M3) — Complete mode keeps ALL current placements fixed and fills only the shortfall; safe because the 30%-clear REMOVES placements (can't create a violation), so a known-good completion always exists. Min-conflicts/repair is deferred to M4. Generate mode (base = pinned only) is implemented but exercised in M4.
- 2026-06-04 (M3) — Cancel: in-engine via `shouldCancel()` (tested in Node); in the UI via `worker.terminate()` (the solve loop is synchronous inside the worker, off the UI thread). Solver budget uses `Date.now()` — affects only WHEN it stops, never the result when it completes, so determinism holds.
- 2026-06-04 (M3) — Soft scoring (S1–S6, default weights) implemented in `solver/score.ts` now (pure, needed for the worker `done` score and M4 candidate compare); wiring soft "amber" badges into the editor + the weight editor UI are M4 deliverables.
- 2026-06-04 (M4) — Candidates are RE-SCORED on the main thread with the live `SoftWeights` (`scoreTimetable` is pure + fast), so changing weights re-ranks instantly without re-solving. The worker only establishes feasibility/placements; ranking is a UI-side, weight-dependent concern. `weightsStore` holds the weights.
- 2026-06-04 (M4) — The 2-day sample is nearly rigid (classes are fully packed), so "generate" still produces feasible candidates but with limited diversity. The diversity AC ("visibly different") is proven against a roomy 6-day variant of the sample (`generate.test.ts`); the live 2-day app still shows distinct candidates (different soft profiles/scores), and real 6-day school data has ample slack.
- 2026-06-04 (M4) — Picking a candidate uses `addDraft` (new timetable, source preserved) and resets editor undo history — consistent with M3's "never overwrite" rule. Weight changes re-rank deterministically (verified: cranking S1 flips the 2nd/3rd candidates).
- 2026-06-04 (M5) — Substitution (`domain/substitution.ts`) is PURE and READ-ONLY — it produces a separate cover plan, never mutates the timetable. An absent ELGA teacher yields a block item with status `owner-decision` and ZERO auto-cover candidates (the AC: a level group can't be silently reassigned). Single-teacher lessons get `needs-cover` with free+qualified candidates (H1/H5/H6/H9) ranked by S1/S4 disruption (no-new-gap then lighter day). Multi-teacher lessons with a present co-teacher are `partial` (proceed, reduced staff).
- 2026-06-04 (M5) — "Absent for a date" maps to a weekly `Day` (the timetable is weekly); a real calendar layer is out of v1 scope. Honest output: when no free qualified teacher exists (e.g. EVS, taught only by the fully-booked primary teachers), the plan says so rather than inventing a cover.
- 2026-06-04 (M5) — Print: a `@media print` rule in index.css hides `.app-shell` + `.no-print` and flattens the `.modal-overlay`, so `window.print()` from the substitution modal prints just the day sheet. The App shell is wrapped in `.app-shell`; modals render outside it. (This print scaffolding also serves M6's print stylesheet AC.)
- 2026-06-04 (M6) — Two print targets via body class: DEFAULT print = the timetable grid (chrome marked `.no-print`, modals hidden); `body.print-subs` = only the substitution day sheet (app shell hidden, modal flattened). The subs print button toggles the class around `window.print()` via an `afterprint` listener.
- 2026-06-04 (M6) — PWA is hand-rolled, NO new dependency: `public/manifest.webmanifest` + `public/sw.js` (cache-first shell on install + runtime caching of hashed assets → offline after first load; navigation falls back to cached index.html). Registered in `main.tsx` PROD-only with `import.meta.env.BASE_URL` so it works under the GitHub Pages sub-path. Icons generated by `scripts/makeIcons.mjs` — a dependency-free PNG encoder (zlib + manual CRC32). Verified in a production `vite preview`: SW registers + controls the page, cache holds shell + JS/CSS, manifest exposes 192/512 icons.
- 2026-06-04 (M6) — Deploy: GitHub Pages via Actions (`.github/workflows/deploy.yml`, runs lint+test+build then `upload-pages-artifact`/`deploy-pages`); relies on `base: "./"` already set in vite.config. Requires one-time repo setting (Pages source = GitHub Actions).
- 2026-06-04 (M6) — Offline FULL-flow fix: the solver worker is the only lazily-loaded chunk, so the SW would only runtime-cache it after the first ONLINE solve — a first-use-offline Generate/Complete would fail. `main.tsx` now warms the worker at startup (instantiate + immediate `terminate`, PROD-only) so its chunk caches while online. Verified in production preview: `solver.worker-*.js` appears in the cache after a plain SW-controlled load with no solve, and the warmed worker still completes a solve.
- 2026-06-04 (M6) — Import (`ExportImport.tsx`) reads files via a `FileReader`-based `readFileText` (works in both browsers and jsdom; `File.text()` was unreliable under the test env). Legacy rawData import is normalized (`normalizeProject`) for consistency with the seeded sample; JSON import goes through `deserializeProject` (schemaVersion guard). The "exported rawData renders in the legacy viewer" AC is backed by the M1 byte-identical round-trip test against the DATA_MODEL.md format spec; the true paste-into-the-live-viewer check is owner-side (no viewer instance available here).
- 2026-06-05 (M7) — First-run no longer auto-seeds a project. `projectStore.init` only loads from IndexedDB; a fresh user gets an empty state with three paths (Set up / Import / Explore demo). `init` early-returns if a project is already in memory (so wizard/import/tests aren't clobbered) and sets an `initialized` flag so the UI distinguishes "loading" from "empty". The two v1 tests that asserted auto-seed (db.test "init seeds", App.test boot) were RE-SPECIFIED (not deleted) to the v2 contract — `makeSampleProject`/`legacyRawSample` (the 2-day fixture for ~60 solver/validate tests) are untouched.
- 2026-06-05 (M7) — Demo dataset is a build-time STATIC snapshot, never solved at load: `fixtures/demoSchool.ts` builds entities + moderate quotas (free periods left → realistic AND easily solvable) via the shared pure `domain/projectBuilder.ts`; `scripts/buildDemoFixture.ts` solves it once deterministically (seed 20260605, 30 s budget) and commits `vpps.demo.ttproj.json`. Solve: 16 classes × 6 days, 322 placements, 0 hard, ~600 ms. Gated by `fixtures/demo.test.ts`.
- 2026-06-05 (M7) — HONEST CLAIM on AC2: no real VPPS `rawData` snapshot exists in this repo, so "importing the real VPPS rawData shows 6 days with zero hard conflicts" is verified against a clearly-SYNTHETIC but faithful 6-day demo (real roster/classes/subjects from SCHOOL_CONTEXT). The real-snapshot import + the v1 round-trip-gap closure remain owner-side.
- 2026-06-05 (M7) — SCHOOL_CONTEXT.md says "Classes (14)" but ENUMERATES 16 (Class 1–10 + 11/12 × Arts/Commerce/Science); AGENTS §6 repeats "14". The enumeration is the concrete reality and "14" is a miscount (10+6=16). The demo honors the enumeration (16 classes) — never invents or drops real classes. Flagged for owner confirmation in HANDOFF.
- 2026-06-05 (M7) — Project assembly lives ONCE in pure `domain/projectBuilder.ts` (used by the wizard and the demo builder); CRUD mutations live in pure `domain/projectEdit.ts` (cascading deletes keep the project consistent — removing a class drops its requirements, lessons and placements). UI (`EmptyState`, `SetupWizard`, `DataManager`) is a thin layer over these + `setProject`. Block CRUD in the data manager is deferred (ELGA arrives via demo/import) — TODO in HANDOFF. Plain-language relabel, Escape-to-close, responsive shell are M8 AC and intentionally NOT done here.
- 2026-06-05 (M8) — Shared `ui/common/Modal.tsx` (Escape + backdrop-click close, role=dialog) — all 5 modals migrated onto it; keeps the `.modal-overlay`/`.modal-card` print hooks. Satisfies the "Escape closes every modal" AC in one place.
- 2026-06-05 (M8) — ELGA-as-band is a pure RENDERING change: `gridModel.buildClassRows` marks a block's origin cell with rowSpan×colSpan (and the rest `covered`) WHEN the block's classes are contiguous in display order; non-contiguous falls back to per-cell. Underlying model stays one `BlockActivity`. Week views merge vertically (rowSpan over periods per day). No domain change.
- 2026-06-05 (M8) — Plain language (rule 8): user-facing strings carry no codes/seeds/scores. Internal API is UNCHANGED (`Violation.constraintId`, solver `seed`, weight keys `S1..S6`) — those are not user-facing, so domain/solver tests stay green. A `uiStore.advanced` flag (toolbar toggle) reveals codes/seeds/scores on demand. "Complete (seed N)"→"Fill the gaps"; "Generate…"→"Create timetables"; candidates→"Option N / Conflicts / Quality"; S1–S6→plain sentences; conflicts→sentences + a "view" jump-to-day link.
- 2026-06-05 (M8) — Week views: pure `ui/grid/weekModel.ts` builds a single class's or teacher's week (rows=periods, cols=days); `WeekGrid` is read-only (used for viewing + printing "a teacher's week"). The editor toolbar+grid+panels were extracted into `GridWorkspace.tsx` to keep `App.tsx` under the 300-line rule. `editorStore` gains `gridView` + `weekScope`; `projectStore` gains `setActiveTimetable`/`deleteTimetable` for the `DraftSwitcher`.
- 2026-06-05 (M8) — Scope vs the 3 ACs: the ACs (five plain-language tasks, Escape-closes-modals, ELGA-as-band) all pass. Non-AC deliverables DEFERRED to M10 polish: left-sidebar nav (a responsive top toolbar is used instead — flex-wrap, sm: padding for mobile), subject color coding, compact-cell popovers, free-slot highlighting. Recorded honestly in HANDOFF.
- 2026-06-05 (M9) — Explainability via a PURE structural pre-check `solver/diagnose.ts` (necessary conditions: teacher weekly demand > cap, teacher demand > days×maxPerDay, class demand > week slots; counts block periods). It names the bottleneck in plain language with a suggestion, WITHOUT running the solver. Shown by `BlockerReport` modal.
- 2026-06-05 (M9) — "Never silently apply an infeasible result": Fill the gaps / Create timetables run `diagnose` BEFORE solving (structural blockers → report, don't solve); if the solver still returns `feasible:false`, show a generic "couldn't fit" report instead of applying; in candidate compare, "Use this" is disabled for any option with conflicts. The solver's worker protocol + determinism-per-seed are UNCHANGED (rule 9).
- 2026-06-05 (M9) — Candidate diff: pure `domain/diff.ts` compares two timetables by class cell (before→after label); CandidateCompare shows a per-option "Changes" count + an expandable "Changes vs the current timetable" list (verified live on the demo: 413 concrete changes).
- 2026-06-05 (M9) — Engine internals NOT rewritten (forward-checking/min-conflicts deferred): the existing seeded backtracker already meets AC1 (full 6-day demo generates feasibly in ~0.6 s, well under 10 s). Rule 9 makes the rewrite optional ("may refactor"); keeping the proven engine preserves determinism + the worker protocol and avoids regression risk. The substantive M9 value is explainability (diagnose), no-silent-apply, and the diff. Revisit forward-checking only if a real over-constrained-but-feasible instance proves too slow.
- 2026-06-05 (M9) — `deriveRequirements` circularity resolved in practice: the wizard + demo produce owner-authoritative `requirements.curriculum` via `buildProject` (no derive). `deriveRequirements`/`normalizeProject` remain ONLY for legacy-rawData import (the sanctioned "infer quotas from an imported timetable" use) and the 2-day test fixture — not fed back into the solver circularly.
- 2026-06-05 (M10) — Soft (amber) badges in the grid: `useDerived` now feeds the grids `scoreTimetable().violations` (hard + soft) instead of just `validate()` (hard). The existing overlay keeps hard-ring precedence over soft. Only S3/S5/S6 have per-cell slots (period ≥ 1) so they color cells; S1/S2/S4 (period 0) stay in the "things to fix" panel as suggestions. The 3 tests that asserted the panel's "No conflicts" message were re-specified to the status header "Ready — no conflicts" (feasibility = no HARD clashes, even with soft suggestions present).
- 2026-06-05 (M10) — a11y: shared `Modal` now traps Tab focus and restores focus to the trigger on close; every icon-only button has an aria-label; dialogs carry role=dialog/aria-modal/aria-label; the Advanced toggle uses aria-pressed. Live audit on the demo: 0 unnamed buttons (of 127), 0 unlabeled fields, html lang + title present, 0 missing alt. HONEST CLAIM: the structural Lighthouse a11y audits all pass (so the score should be ≥ 90), but the exact numeric Lighthouse score is to be confirmed on the deployed site (no Lighthouse runner in this environment) — same honesty discipline as the legacy-viewer check.
- 2026-06-05 (M10) — Four print layouts via the established `@media print` targets: master grid (whole-school day view), per-class week sheet (week view, class scope), per-teacher week sheet (week view, teacher scope), substitution day sheet (`body.print-subs`). The WeekGrid lives outside `.no-print`, so selecting a scope then Print produces that sheet. Coach-marks tour / undo toast / autosave indicator were NOT built (optional polish, not AC) — noted for a future pass.
- 2026-06-05 (M11) — Storage resilience: every `persistence/db.ts` op is wrapped in a `withTimeout` (3 s default, `STORAGE_TIMEOUT_MS`) so a wedged `indexedDB.open` (blocked upgrade held by another tab) rejects with `StorageTimeoutError` instead of hanging forever. On timeout the memoized connection is dropped (`resetDbConnection`) so "Try again" is a genuinely fresh open. `projectStore` gains `storageStatus: loading|ready|error` + `saveFailed`; `init` catches storage failure → `error` (never strands on "Loading…"). A `RecoveryScreen` offers Try again / Download a backup / Start fresh.
- 2026-06-05 (M11) — Save-hang vs save-reject (advisor flag): the autosave banner is wired to the timeout (option a) — `saveProject` is itself timeout-bounded, so a wedged open *rejects* in `scheduleSave` and flips `saveFailed` on, rather than silently hanging. Banner is non-blocking (amber bar above the header) with a one-click "Download a backup".
- 2026-06-05 (M11) — "Download a backup" is timeout-bounded too: storage-error variant re-reads via `readBackup()` (fresh timed `loadProject`); if still unreadable it says so instead of re-bricking. The corrupt-data variant (project loaded but active timetable id dangles — previously a second infinite "Loading…" path at App.tsx) backs up the in-memory project directly, then offers Start fresh.
- 2026-06-05 (M12) — Combined senior sections: when ≥2 classes share an IDENTICAL `(subject, teachers)` cell in the same slot (the senior streams' joint Hindi / English compulsory / Economics), `legacyImport` now merges them into ONE length-1 multi-class `BlockActivity` instead of N independent single-class lessons. Reuses all existing block machinery (derive counts the teacher once → no false H1; export reproduces the per-row text; M8 band-rendering shows the combined band). Verified against the real snapshot: 92 such combined-section slots, **0 genuine cross-subject clashes**, so the merge is safe and the published timetable validates at 0 hard. Rationale for block-not-Lesson: `Lesson.classId` is singular; reusing the block path is a zero-domain-type-change, rule-9-safe model and a combined section IS atomic (you can't split one stream's Hindi from another's). H6 (qualified) isn't checked on blocks — an accepted, pre-existing trade-off (ELGA already bypasses H6).
- 2026-06-05 (M12) — Real snapshot is the data spine: `src/fixtures/vppsReal.ts` imports `docs/sources/rawData.vpps.txt` verbatim via Vite `?raw` (works in vite build AND vitest) — single source of truth, no committed-JSON copy to drift. `makeRealVppsProject()` = import + `normalizeProject`. The app's "Explore demo" (`loadDemo`) now loads the REAL VPPS school; the synthetic `makeDemoProject()`/`vpps.demo.ttproj.json` is retained TEST-ONLY (sanctioned by ROADMAP M12) as the representative 16-class project ~15 solver/grid/edit tests depend on. The long-deferred semantic round-trip test landed (`vppsReal.test.ts`).
- 2026-06-05 (M12) — Semantic (tolerant) round-trip: the live viewer separates day blocks with a blank line; `exportLegacyRawData` doesn't. That's the ONLY difference, so the round-trip test normalizes (drop blank lines, trim trailing space) and compares content lines. Closes the v1/v2-deferred real-data round-trip gap; the byte-exact M1 test against the canonical format still stands for the synthetic 2-day sample.
- 2026-06-05 (M12) — Rule-13 doc fixes from the snapshot: ELGA runs **Mon–Thu** (not the demo's "Mon+Thu"); roster is exactly **18** teachers (not "~19"). SCHOOL_CONTEXT updated + open questions 1/3 answered. The synthetic demo's "Mon+Thu" ELGA is now test-only so it doesn't mislead.
- 2026-06-05 (M12) — Inferred-quota review (AC3): legacy import no longer commits immediately — it shows `ui/manage/QuotaReview` (per-class list of inferred subject + teacher + editable periods/week, with per-class "X of Y periods/week planned" incl. block periods, integer inline validation, rule-12 number inputs not text). Confirm applies edits via pure `setQuotaPeriods` then commits; Cancel discards. The demo path skips review (already-confirmed real data).
- 2026-06-05 (M13) — Pages, not modals: hand-rolled hash routing (`useHashRoute`, ~20 lines) + a persistent `Sidebar` (7 views: Timetable/Teachers/Classes/Subjects & Quotas/Blocks/Substitutions/Settings). No react-router (would need an AGENTS §1 justification; hash routing also avoids Pages sub-path 404s). Management lives in pages (`ManagePages`: Classes/Teachers/Settings/Blocks + `QuotaMatrix`), `SubstitutionView` became a page, and the `DataManager` modal was deleted. Modals remain only for the wizard, File (export/import), Create timetables, and confirmations. Mobile: the sidebar is a horizontally-scrollable bar (every view reachable at 390px — verified live); we did NOT gold-plate the 16×22 matrix for phones (sticky-column horizontal scroll is the mobile story).
- 2026-06-05 (M13) — Quota matrix (`QuotaMatrix`): classes × subjects number cells, per-cell teacher `<select>` (rendered only when periods > 0 to keep the DOM light), per-class running total vs available slots (block-aware), and bulk tools — copy one class's quotas to all, fill a subject column. Backed by pure, tested `setClassSubjectQuota` (replaces a cell even when the teacher changes), `copyClassQuotas`, `fillSubjectColumn`. Replaces the one-row-at-a-time entry the review flagged as impractical at ~100 rows.
- 2026-06-05 (M13) — Wizard v2: chips (`ui/common/Chips`) replace the classes textarea and the comma-separated subjects inputs (zero textareas for structured entry — verified by test); per-step inline validation explains why Next/Finish is disabled; a **Blocks step** (net-new, shared `BlockForm` + pure `addBlock`/`removeBlock`) delivers the ELGA definition M7 originally promised; progress autosaves to localStorage via `persistence/wizardDraft` (kept out of the IndexedDB project store — drafts ≠ finished projects). The wizard's old one-row Quotas step was DROPPED: finishing routes to the quota matrix (`#/quotas`), where bulk tools fill weekly subjects — no redundant per-row entry.
- 2026-06-05 (M13) — "<15 min" AC is not wall-clock-testable, so it's a *completability* walkthrough (`domain/quotaWalkthrough.test.ts`): a skeleton + ELGA block + 5 bulk calls produce quotas for all 16 classes (64 requirements), proving the tools — not 100 rows — do the work. Print model simplified: chrome is `.no-print`, so the active view (grid / week / subs sheet) is the print target; removed the `body.print-subs` body-class juggling (subs is a page now).
- 2026-06-05 (M14) — One source for "planned vs slots" (advisor flag): exported `demand()` + added `classLoads()` to `solver/diagnose.ts`; the quota matrix, the header hint, and the pre-flight all read it, so the number can't diverge across screens. The under-quota check is the mirror of diagnose's existing over-capacity check (`slots − need`).
- 2026-06-05 (M14) — `solver/guidance.ts` (pure): `nextStep` (priority: fix conflicts → add quotas → resolve a structural blocker → plan free periods → ready) drives the header hint; `preflight` builds the pre-flight checklist. KEY RULE: under-quota is a WARNING (free periods are valid), only no-quotas / over-capacity / teacher-overload BLOCK — so a deliberately under-quota'd project still reaches generate→print (AC#3). Lives in solver/ (not domain/) because it imports diagnose; UI imports it (ui ← solver, allowed).
- 2026-06-05 (M14) — Pre-flight on the Create CTA only: `CandidateCompare` shows `PreflightChecklist` before generating and disables "Create options" unless `preflight.ok`. "Fill the gaps" keeps its existing M9 diagnose→BlockerReport (advisor: don't expand). Guided tour (`Tour` + `uiStore.tourOpen`, persisted via `persistence/uiPrefs` localStorage) is a step-through modal (NOT element-anchored coach marks — brittle, not in AC), auto-opens once on first run, replayable from Settings. Glossary `?` popovers (`ui/common/Glossary`) for quota/block/draft/pin — no first-encounter tracking (not in AC, where it balloons).
- 2026-06-05 (M14) — Test env note: jsdom `localStorage` round-trip is flaky under the node-env-overridden jsdom (`--localstorage-file` warning), so the tour test asserts the in-memory `uiStore.tourOpen` gate (robust) rather than the localStorage flag; the cross-session persistence is verified live in the browser. Global test setup marks the tour "seen" so it doesn't overlay other App tests. AC#3 ("non-technical tester completes import→adjust→generate→print unaided") is human/owner-side — the verifiable parts are the hint + pre-flight + tour unit tests and the live walkthrough.
- 2026-06-05 (M11) — DEVIATION from ROADMAP "deleteDatabase + reload": `startFresh` does `deleteAllData()` (best-effort, timeout-bounded `deleteDB`) then resets the store to a clean empty `ready` state IN MEMORY instead of `window.location.reload()`. Rationale: `location.reload` is unavailable/awkward under jsdom (untestable), and an in-memory reset reaches the same working empty state deterministically and is unit-testable (AC2). A blocked delete still lands the user on a usable empty state; the recovery tip tells them to close other tabs.
- 2026-06-05 (M15) — Rule type = discriminated union on `template` (R1Rule…R15Rule), NOT a generic `params` bag — a bag forces casts/`any`; the union is the only no-`any` shape (advisor-confirmed). Doc-first honored: `docs/DATA_MODEL.md` Rule addendum + `src/domain/types.ts` landed in the same commit. types.ts intentionally exceeds the ~300-line guideline — AGENTS §1 mandates it be the SINGLE domain-types file, which overrides the split rule.
- 2026-06-05 (M15) — Engine layering: `domain/rules.ts` is the thin engine (wrap + must/prefer entry points); the 15-case predicate switch lives in `domain/ruleChecks.ts` (`hitsFor`); shared per-class/per-teacher occupancy stats in `domain/occupancy.ts`; one sentence renderer `domain/ruleText.ruleSentence` feeds BOTH the Violation message and the M16 UI label (never two phrasings). Split purely to keep each file near the budget; the switch is one cohesive unit so it isn't split mid-way (ruleChecks 312, validate 319 — marginal, single-construct).
- 2026-06-05 (M15) — Single source of truth for denormalized facts: structural facts live on entities — `SchoolClass.classTeacherId`/`isBoardClass`, `BlockActivity.allowedDays`/`fixedStartPeriod`. Rules R4/R7/R9 carry ONLY rule-specific config and READ the entity field (validate enforces one path). EXCEPTION: R8 carries its own `slots` as the rule-layer complement to `Teacher.unavailable`/H5 — two distinct intents (baked-in availability vs a rule the user adds), not a duplicated source.
- 2026-06-05 (M15) — Operationalizing spec-fuzzy rules: R9 "board-class protect" → "no non-core subject in periods 1–3 for a board class" (gated by `isBoardClass`, default `prefer`) — the analysis only says "core gets priority", so this is the simplest observable that yields a satisfied/violated test. R13 "compact days" → one violation per teacher idle-gap (mirrors S1) as a global `prefer` rule. Both recorded here per the advisor's "pin it, don't invent semantics the analysis can't support" note.
- 2026-06-05 (M15) — Durations: `Lesson.duration` (1 default, 2 = double) flows through `occupiedPeriods`, so a duration-2 lesson moves/deletes as ONE unit and clash-checks for free across all callers. H4 generalized to "any multi-period unit fits inside the day" (blocks + double lessons); H8 now counts subject-PERIODS not placements (a double = 2/day). Solver PLACEMENT of duration-2 is deferred to M17 — the solver only creates duration-1 requirement lessons, all current fixtures are duration-1 (asserted: full suite green, no regression). legacyExport emits a double as two cells automatically (it reads per-period expanded occupancies).
- 2026-06-05 (M15) — Scoring wiring: `must` rules join `validate()`'s hard count; `prefer` rules are scored in `scoreTimetable()` with THEIR OWN `rule.weight` summed separately — the existing `SoftWeights` reducer is keyed S1–S6 and would score an `R*` constraintId as 0. R11/R12/R13 deliberately overlap H8/H9/S1 (v4's direction is to migrate the fixed set into rules); they reuse `domain/occupancy.ts` helpers so counts can't diverge (advisor's drift flag).
- 2026-06-05 (M15) — `schemaVersion: 2` + `migrate` v1→v2 (adds `rules: []`, all new entity/profile fields optional so no rewrite). Back-compat proven on the committed v1 JSON fixtures (`vpps.demo`/`vpps.sample`) which load via `deserializeProject`→`migrate`; a synthetic v1 file is also asserted to migrate (rules.test). Unknown versions still throw.
- 2026-06-05 (M16) — Detection proposal model = explicit `{ rule, sentence, entityUpdates: EntityUpdate[] }` (NOT a closure), and ONE entity-aware add (`ruleEdit.addRuleWithBacking`) serves both detect-accept and the manual builder — accepting R4/R7/R9 patches the structural fact the rule reads (classTeacherId / block allowedDays+start / isBoardClass). Avoids the two-path drift class flagged in M14.
- 2026-06-05 (M16) — Detected P1 anchors are `prefer` (soft), NOT `must`: the pattern is strong but real data has off-days (Class 6: Hemlata is in P1 daily but the subject varies; other classes' anchor teacher is there only a majority of days). A `must` anchor would turn a clean import into a conflicted one — wrong for propose-and-confirm. Subject is fixed on the anchor only when it's unanimous across the teacher's P1 days (else teacher-only). Guaranteed by test: accepting ALL detected rules keeps the real timetable at 0 hard conflicts. A hand-authored R4 still defaults to `must`.
- 2026-06-05 (M16) — R7 detection only proposes for GENUINE multi-period blocks (`length ≥ 2`, i.e. ELGA). The M12 combined senior sections (joint Hindi/Economics/English) are modeled as length-1 blocks but aren't "runs only on these days" constraints — without the guard, detection spammed a meaningless R7 per joint lesson (caught live). R6 doubles detect consecutive same-subject+teacher runs (matches imported singles, not `duration===2`), proposed only when recurring on ≥2 days.
- 2026-06-05 (M16) — MIGRATION-ON-LOAD FIX (caught live, important): `persistence/db.loadProject` now runs `migrate` on the stored record. A project persisted under v1 (no `rules`) was loaded raw and crashed the app the moment any UI read `project.rules` (`.filter` of undefined) — a real returning-user bug, not just test data. The file path already migrated via `deserializeProject`; the IndexedDB path now does too.
- 2026-06-05 (M16) — Rule builder = ONE generic field-schema (`ui/manage/ruleFields.ts`, ~8 reusable field kinds: subject single/multi, class single/scope, teacher, period-set, day-set, half, number, block-ref, core-subject-multi) + a thin renderer (`RuleBuilder`) with a prominent LIVE `ruleSentence` preview (the sentence-first bar, rule 15 — verified live: "Mahesh is class teacher of Class 11 Science and takes period 1 daily" forms as fields fill). R4/R7/R9 are special-cased to emit `entityUpdates` rather than generalizing a field-target system for the 12 that don't need it. All seven VPPS families proven expressible by a buildRule unit test.
- 2026-06-05 (M16) — Violations panel groups R* rule violations under a header (the rule's own `ruleSentence` when exactly one enabled rule of that template exists, else the template title); H/S stay flat with click-to-jump. Presets (`indianK12Defaults`) are guarded against subjects/teachers the project lacks and applied via a RulesPage button (the "optional at setup" path). New "rules" view wired in three spots (useHashRoute VIEWS, Sidebar, App switch).
- 2026-06-05 (M17) — Targeted regenerate uses VARIABLE-RESTRICTION, NOT an engine change: `withClearedScope` removes only the scope's non-pinned single-class lessons (blocks + pinned stay frozen — clearing a primary class keeps ELGA), then the EXISTING complete-solve through the EXISTING worker refills exactly those (shortfall == cleared lessons), so only the scope's cells change. Preserves the rule-9 determinism/worker-protocol contract. Guarantee is conditional on the baseline meeting quota — verified: the real VPPS import has 0 short requirements (11 excess, harmless). Test asserts changed-cells ⊆ scope on the real import (not just eyeballed). Day-scope is the one scope variable-restriction can't bound (cleared Monday lessons could land Tue–Sat) — DEFERRED (would need a serializable `restrictToDays` on the protocol); class/teacher/subject ship.
- 2026-06-05 (M17) — A tight, heavily-coupled scope (Class 7/8 — their teachers are booked across many frozen classes) may not fully refit within the solver budget; the result comes back `complete:false` and `RegenerateControl` SURFACES it ("couldn't refit every period — try again or pick a smaller scope") rather than silently dropping. Verified live on Class 8: 16 cells changed, all Class 8, 0 new problems, partial-refit message shown. This is a backtracker-strength limitation, not a scope-containment bug (the AC — containment — holds).
- 2026-06-05 (M17) — Swap finder (`legalSwaps`) candidates = exchanges with another single lesson sharing the class OR a teacher (the everyday "teacher asked to switch"); blocks and duration-2 lessons excluded. The property test uses an INDEPENDENT oracle — it applies each returned swap (`applySwap`) and re-runs the real `validate()` asserting 0 hard (not the same filter the finder used) — plus a negative case (a known-clashing swap is excluded). DRY: ledger fixed/created, impact preview, and swap-check all share one `hardKeySet` (key = constraintId + serialized slots); fixed/created come from set-DIFFERENCE, not count subtraction (a move can fix one H1 and create another → "1 fixed, 1 created", net 0).
- 2026-06-05 (M17) — Branch/compare/promote: a session-only `scenarioStore` holds `{ baseId, branchId }` — NOT a `sourceId` on the domain `Timetable` (that's schema churn + migration for transient session state). Branch = `addDraft` with a placement copy; promote routes through `editorStore.replaceActivePlacements` (snapshot live → commit branch's placements) so the single global Undo reverts the promote (verified live). `editorStore` gained `swap` + `replaceActivePlacements` (both undoable); grid cells gained click-to-select (drag activation distance 4 means a plain click selects, doesn't drag). Impact-on-hover + the "free-slot prose" were timeboxed OUT (not in the AC).
- 2026-06-05 (M18, partial) — Read the authoritative `Class_Wise.pdf` (all 16 pages) and compared it to the `rawData.vpps.txt` import across primary/middle/senior/board classes. FINDING: the cells already match in subject+teacher; the ONLY differences are (a) subject-label verbosity, (b) the four board flags, (c) the clock + break the rawData subset drops. So reconciliation is additive, not corrective.
- 2026-06-05 (M18, partial) — `domain/reconcile.ts` (pure) applies the PDF's structural facts the import omits: real per-period heatwave clock (P1 07:30 … P6 11:45) + the break after P4 (10:10–10:25) onto `ScheduleProfile`, and `isBoardClass` on exactly the four "Board class priority protected" pages (Class 10, 12 Arts/Commerce/Science — the 11 streams are NOT board). Applied in `makeRealVppsProject` AFTER normalize. It does NOT touch cell data (placements/subjects unchanged — asserted idempotent), so the byte-exact legacy round-trip stays green. Subject-label differences are a DISPLAY/compare ALIAS map (`PDF_SUBJECT_ALIASES`: "English compulsory"→"English", "Core Revision"→"Revision", "English Literature"→"Eng. Lit.", "Political Science"→"Pol. Sci.", "Business Studies"→"B. Studies", "English Revision"→"Eng. Revision"), NOT a rename — renaming would break the round-trip which must reproduce rawData verbatim. Verified the board-class P1 anchors cell-for-cell against the PDF under the alias map (`reconcile.test.ts`).
- 2026-06-05 (M18, partial) — Entity-lifecycle reassignment (AC#2): `domain/lifecycle.ts` `reassignTeacher` moves EVERY reference (lesson/block `teacherIds`, requirement `teacherIds`, `SchoolClass.classTeacherId`, and the teacher-scoped rules R8/R12/R15) from one teacher to another, unions the inherited subjects onto the target (so H6 stays satisfied), then drops the old teacher — leaving ZERO dangling references (asserted on the real VPPS project by scanning all five reference sites). `teacherImpact` powers the pre-delete preview. UI `ReassignTeacherModal` replaces the bare ✕ on the Teachers page with "Remove {name}? teaches N periods… reassign to whom?" + reassign / remove-anyway. NOTE: reassigning a full load onto an existing teacher legitimately creates load/clash conflicts (verified live: Maya→Anjana → 3 H-clashes) — that's a real downstream concern surfaced in the conflicts panel, NOT a dangling-reference bug; the AC (no dangling ref) holds. Rename was deferred (ids == names per the M1 decision, so a true rename rewrites the id everywhere — separate change; the AC#2 is specifically remove→reassign).
- 2026-06-05 (M18) — Cell-for-cell PDF match (AC#1 ✓): transcribed all 16 classes × 6 days × 6 periods of `Class_Wise.pdf` into `fixtures/classWisePdf.ts` (independent fixture). `classWisePdf.test.ts` builds the grid from `makeRealVppsProject` and asserts every cell equals the fixture under `pdfSubjectLabel` (ELGA = the block; M12 combined senior sections are blocks carrying a real subject, rendered like a lesson for compare). The cross-check caught exactly ONE real PDF-vs-rawData label diff across all 576 cells — rawData "Science Practice" vs PDF "Sci. Practice" (4 cells) — added to `PDF_SUBJECT_ALIASES` (PDF wins, rule 16). All 576 now match: the transcription is accurate AND the import equals the printed truth cell-for-cell.
- 2026-06-05 (M18) — Print parity (AC#3): the per-class week, per-teacher week (both `WeekGrid`), and whole-school day grid (`TimetableGrid`) now render the reconciled per-period clock and the positioned break ("Break · 10:10–10:25" after P4), mirroring the three Class_Wise/Teacher_Wise/Day_Wise PDF formats. Verified live (week view: P1 07:30–08:10 … P4 09:30–10:10, then the break row). HONEST CAVEAT (same discipline as the Lighthouse/legacy-viewer claims): the views now structurally match the PDF format; pixel-exact "looks identical" is an owner-side visual check.
- 2026-06-05 (M18) — M18 AC MET: (1) in-app grid matches Class_Wise.pdf cell-for-cell (576/576, scripted); (2) teacher removal walks through reassignment with zero dangling references; (3) the three print views carry the PDF's clock + break + formats. **v4 (M15–M18) complete.** The period-count-change wizard was a ROADMAP feature bullet, NOT one of the three AC clauses — PARKED as a post-v4 follow-up (HANDOFF), alongside the existing parked list (rooms/labs, multi-school, preference forms, stats dashboard, share links, versioning).
- 2026-06-05 (M19) — Zero-setup bundled default: `src/fixtures/bundled.ts` is the single source of the built-in school — `buildBundledProject()` = `makeRealVppsProject()` + accept ALL `detectRules` proposals (16 R4 anchors, 26 R6 doubles, 1 R7 ELGA-config) + stamp `bundledDataVersion`. Accepting the lived-by rules keeps the real timetable at 0 hard conflicts (asserted) — only R7 is `must` and it matches the actual ELGA slots. `init()` now SEEDS this automatically when storage is empty (a fresh/cleared browser opens straight into the real grid, rules on, 0 conflicts, NO user action); `startFresh` re-seeds it too (recovery still lands on a working school, not a blank welcome). The empty-state/wizard moved behind Settings "Start a different school".
- 2026-06-05 (M19) — `bundledDataVersion?: number` added to `Project` (doc-first: `types.ts` + `DATA_MODEL.md` same change) — OPTIONAL, no `schemaVersion` bump (consistent with how v2 added optional fields). Stale detection `isStaleBundled` = `school.name === VPPS && (bundledDataVersion ?? 0) < BUNDLED_DATA_VERSION`. COUPLING-TO-SCHOOL-NAME is deliberate and documented: a user's own school (different name) never sees the update banner; the escape hatch is "Start a different school". A returning v4 browser holds an unstamped VPPS project → `0 < 1` → banner. Bump `BUNDLED_DATA_VERSION` on ANY change to the bundled data/rules (Prompt F rule 19).
- 2026-06-05 (M19) — "Never overwrite without keeping a draft" (rule 19) = keep the WHOLE old project, NOT a placement-merge (advisor call). `adoptBundled` saves the current project under a `previous:<iso>:<n>` IndexedDB key FIRST (aborts, leaving current untouched, if that save fails), then swaps in the fresh bundled project; `lastPreviousKey` drives one-step Undo, `restorePrevious`/`deletePrevious` + a Settings "Saved drafts" list manage them. Placement-merge was rejected: it loses entity edits (data-manager/M18 reassignment) and risks dangling `activityId`s when the rebuilt project's ids diverge — and the AC "keeps the old timetable as a draft" is satisfied because the kept project CONTAINS that timetable. The stale banner (Update/Not now) + the Settings "Reset to school timetable" both funnel through `adoptBundled`, so the reset is undoable via the same kept draft. Verified live: pre-v5 project → banner → Update → current=v1/43-rules, old kept under `previous:` key, Undo offered.

- 2026-06-07 (v6 REBUILD) — Full rewrite on an EVENT model (TimetableEvent spanning many classes + many teachers), replacing the cell model — the cell model cannot represent senior combined classes (joint_class: 11/12 share English/Hindi/Economics) or ELGA (team_block: 5 classes × 5 teachers); it false-flags them as clashes, the root cause of "feels off". Owner-decided after two fresh real-timetable analyses (docs/sources/VPPS_Timetable_*Analysis*_2026-27.md).
- 2026-06-07 (v6) — Default skeleton switched to the REAL 8-period 2026-27 day (Assembly + P1–P8 + Recess); 6-period heatwave becomes a secondary switchable profile. Owner wants the app to open to the real timetable so they only tweak.
- 2026-06-07 (v6) — Editing is LEGAL-ONLY: the slot picker offers only qualified, conflict-free options (Qualification table + availability + same-slot occupancy + must-rules). Mistakes are designed out, not reported. Owner choice over "free edit + warnings".
- 2026-06-07 (v6) — Clash redefined: an overlap is a real clash only if the two occupancies come from DIFFERENT eventIds; same-event overlap (joint/team) is legal by construction. This is the single fix for the false-clash problem.
- 2026-06-07 (v6) — Rebuild happens IN-PLACE in this repo (keep tooling, deploy workflow, docs, git history); src/ rebuilt module-by-module on a branch. Proven validation/solver logic is re-derived on the event model, not carried as-is. M19 (6-period bundle) shipped just before the pivot and will be replaced by RB1.

- 2026-06-07 (RB0) — REBUILD started on a `rebuild` branch (NOT main): main auto-deploys to GitHub Pages on push, and RB0–RB1 leave the app a placeholder until the editor returns in RB2; accumulating RBn on a branch and merging at parity keeps the live working timetable up. Big-bang delete of cell-model src/ (domain/solver/store/ui/worker/persistence + cell fixtures) — git history is the recovery net; proven validation logic re-derived on the event model, not ported. Kept: tooling/config/docs, `src/test/setup.ts`, `src/index.css`, and `src/fixtures/classWisePdf.ts` (the standalone 6-period real cell transcription — raw material for RB1's Qualification table + transcription).
- 2026-06-07 (RB0) — Profile slots are physical-time-ordered with `index` = array position, so Recess is index 5 and teaching slots are non-contiguous `[1,2,3,4,6,7,8,9]`. A duration-d event occupies its start slot + the next d−1 TEACHING slots, skipping fixed slots (ELGA P3×3 = `[3,4,6]`, straddling Recess). DATA_MODEL's loose "Assembly,P1..P8,Recess / 1..8 teaching" shorthand is clarified to this physical model (doc updated § "Slot indexing"). The analysis clock (§1) is ground truth and matches exactly.
- 2026-06-07 (RB0) — Clash = >1 DISTINCT eventId among the occupancies at a (entity, day, slot); `derive.distinctEventIds`. Same event placed twice in a slot, or a joint/team event's many classes/teachers, collapse to one eventId → 0 clashes by construction. Test asserts BOTH directions (joint+ELGA → 0; two different events on one teacher → exactly 1 HE1) so a regression in real-clash detection can't pass.
- 2026-06-07 (RB0) — Carried v2 Rule catalog renamed to event-model position terms (`periods`→`slots`, `period`→`slot`, `blockId`→`eventId`, `SlotRef.period`→`SlotRef.slot`) for consistent indexing into `Profile.slots`; semantics unchanged, rules not wired into validate() until RB6. Violation slot ref uses `slot` + optional `eventId`. DATA_MODEL § "Carried rule catalog" + "Validation result shape (event model)" updated in the same commit (doc-first).
- 2026-06-07 (RB0) — HE3 (qualification) applies the legal-move rule to every event with teachers, team_block included: each teacher must be qualified for every (subject, class) the event spans (ELGA ⇒ 5×5 teacher×class triples). Zero-teacher events (free/self_study) are exempt. HE6 (integrity) is a shape check on events (normal=1 class; joint_class ≥2 classes+1 teacher; team_block ≥2 classes+≥2 teachers) since a single placement structurally places all of an event's classes/teachers together. HE8 (pinned immovable) has no static form — enforced in the solver (RB5).
- 2026-06-07 (RB0) — FORWARD FLAG for RB1: the only cell-level real-data source in-repo (`classWisePdf.ts`, `rawData.vpps.txt`) is 6-PERIOD (16×6×6=576); the two 2026-27 analyses describe the 8-period grid only structurally (weekly counts + ELGA/joint/availability facts), not as a full 8-period cell grid. RB1's "fixture test matches the source views cell-for-cell" is therefore underspecified for 8 periods — resolve at RB1 start (owner question or a documented reconstruction from counts + structural facts). Do NOT discard the 6-period data.

- 2026-06-07 (RB1) — Owner supplied the REAL 8-period 2026-27 timetable (3 PDFs: Class/Teacher/Day-wise). Confirmed it IS 8-period (Assembly + P1–P8 + Recess 11:10–11:30), unlike the in-repo 6-period heatwave set. Transcribed via coordinate-bucketing with PyMuPDF (build-time tool, not an app dep): render-and-read was unreliable for the stacked subject/teacher cells and the ELGA overlap artifact ("REaLvGinAa" = ELGA label overlapping "Ravina"), so words are bucketed into (day-row, period-col) by header-x / day-label-y boundaries. Output committed as `src/fixtures/realGrid.ts` (16×6×8 tokens, DATA ONLY) — the 8-period analogue of classWisePdf.ts (which stays as the heatwave reference).
- 2026-06-07 (RB1) — Transcription cross-validated three ways before trusting it: (1) source grid is teacher-clash-free (0 double-book signatures); (2) ELGA is always exactly the primary five and Mon–Thu P3–P5; (3) every teacher's weekly teaching-period count matches the complexity-analysis §6 workload table EXACTLY (18/18) — and that table was derived from the Teacher-Wise view, so class-wise and teacher-wise agree. An early VISUAL read of Class 2 wrongly saw Friday ELGA; coordinate extraction proved it's EVS/CCS — vindicating the tooling over eyeballing.
- 2026-06-07 (RB1) — `domain/buildProject.ts` (pure) folds the cell grid into EVENTS: repeated single-class cells collapse to one normal event placed many times; cells sharing (subject, teacher, day, slot) across >1 class become ONE joint_class; ELGA becomes ONE team_block (duration 3, one placement per ELGA day at P3, pinned). Joint membership is stable, yielding exactly 6 joint events (English/Hindi × {11 triple},{12 triple}; Economics × {11 Com+Arts},{12 Com+Arts}). Joint + team events are pinned; normal/special are movable. `domain/gridReconstruct.ts` reverses project→grid; `realGrid.test.ts` asserts all 768 cells round-trip exactly. `fixtures/bundled.ts` holds the VPPS metadata (bands/streams/board, Mahesh P4–P8 + Anjana P1–P4 unavailable, Director non-schedulable, subject kinds, ELGA team) and `buildBundledProject()`; `BUNDLED_DATA_VERSION = 1`.
- 2026-06-07 (RB1) — Availability windows DERIVED from the grid (Mahesh teaches only P1–P3 → unavailable P4–P8; Anjana only P6–P8 → unavailable P1–P4 per analysis "available after recess"). Board classes = {Class 10, Class 12 Sci/Com/Arts} (CBSE board years + M18 precedent); the PDF's "Board classes highlighted" colour was ambiguous to auto-detect (the fills are ELGA/Free cell shading), so this is data, refinable in RB6. Subject bands derived from class usage → Science split falls out automatically (Physics/Chemistry/Biology = middle+senior, combined Science = secondary).
- 2026-06-07 (RB1) — `validate(buildBundledProject())` returns ZERO hard violations (HE1–HE7), proving the real timetable is clash-free under the event model AND that every placed (teacher,subject,class) — incl. the 25 ELGA team triples — has a qualification. Added a read-only RB1 shell (`ui/app/App.tsx` + `ui/grid/WeekGrid.tsx`): the app now OPENS to the real timetable with a class picker; ELGA renders amber, joint senior classes violet, both as single events. Live-verified (Class 1 matches the PDF; Class 11 Science shows joint English/Pradhyuman in violet + Mahesh early-only). RB2 makes it editable (legal-only picker, class/teacher/day toggle).

- 2026-06-07 (RB2) — Legal-only editor built on a PURE core (advisor-guided): `domain/legalMoves.legalOptions` (the legal-move rule) + `domain/edit.ts` (placement-granular, immutable). The picker offers ONLY qualified+available+free options; proven by an INDEPENDENT-ORACLE test (apply each offered option → validate() reports 0 new hard, across 3 classes × 6 days × 8 slots) asserting BOTH sides (Director/Mahesh-after-recess/busy teachers never offered). Edits never mutate a shared event in place (an event has many placements) — clearCell removes the whole covering placement (a joint/team cell clears for all member classes; other placements survive), placeNormalLesson = clear + ensureEvent(by signature) + add placement. Edit-isolation tested (replacing one cell changes exactly 1 of 48).
- 2026-06-07 (RB2) — Single store edit path (`store/projectStore.ts`, Zustand, in-memory; seeds buildBundledProject; global undo via project snapshots). Class and Teacher views are both projections of `deriveMaps`, so an edit in one shows in the other (store test + live-verified: placing Sports/Rakesh for Class 11 Commerce via the picker appeared in Rakesh's teacher view). Emptying a cell = remove the placement (settled). UI: `WeekGrid` (clickable, keyboard-accessible cells), `TeacherGrid` (read-only, joint/team shown as one cell across all classes), `CellPicker` (legal options + "Explain this cell" + guards shared cells with an explicit "remove for all N classes" — never a silent half-replace), `panels/Insights` (class health dot + teacher load bar). Live-verified end-to-end; persistence (IndexedDB) deferred to a later step (RB2 AC needs none).
- 2026-06-07 (RB2) — RB2 gating AC MET (picker-legality scripted both ways; multi-view consistency; keyboard + mobile-readable via focusable cells + overflow-auto/responsive layout). The starred niceties ghost-autocomplete and drag-with-auto-swap are NOT yet built — they ride on the proven `legalOptions` + edit primitive and are the next RB2 increment before merge to main.
- 2026-06-07 (RB2 niceties) — Drag-with-auto-swap built on a PURE finder (`domain/swaps.ts`): `canSwap(project, tt, cellA, cellB)` is the atomic predicate both the drag path and a "show legal swaps" list ride on. It applies the exchange via a new `edit.swapPlacements` primitive (swaps both placements' (day,slot) in one immutable pass — events untouched) and accepts ONLY if the hard-violation count does not increase (validate() is the oracle, same bar as the picker; using ≤before rather than ==0 so it stays correct on a non-zero baseline). SCOPE: swaps stay within ONE class's week and only exchange two NORMAL lessons — a shared joint/team cell is never swapped through this path (it would silently move every member class; the picker handles those explicitly). Proven two-sided: every offered swap on the bundled data keeps 0 hard AND swaps actually exist (non-vacuous), plus discriminating synthetic negatives — a swap that would breach availability (HE4), double-book a teacher (HE1), or push a double period off the end of the day (HE7) is refused, with a no-block control proving the test discriminates.
- 2026-06-07 (RB2 niceties) — Ghost autocomplete built on `domain/ghost.ghostSuggestion`: the faint best-legal suggestion for an EMPTY teaching slot. "Best" = the legalOptions() candidate whose subject has the largest positive weekly shortfall (requirement periodsPerWeek − periods already placed; a duration-2 lesson counts 2), tie-broken by the option's label (legalOptions is label-sorted, so deterministic). Legality is FIRST — the ghost is always one of legalOptions(), so it can never be unqualified/clashing; shortfall only ranks among legal candidates. Returns null for occupied/non-teaching slots and when nothing legal addresses a real shortfall (so it never suggests pushing a class over quota). Since bundled requirements == placed counts, a ghost only appears after the owner clears a cell — exactly when a suggestion is wanted. Tested: suggests the now-short subject after a clear and the suggestion is legal; null on occupied/Recess; deterministic.
- 2026-06-07 (RB2 niceties) — Drag-with-auto-swap WIRED via @dnd-kit/core (already a dep): the class WeekGrid wraps in a DndContext (PointerSensor, 5px activation so a click still selects), each normal lesson is draggable and every teaching cell droppable (id = "${day}#${slot}"); a shared joint/team cell is NOT draggable (would silently move every member class). Drop resolves through one store action `tryDrop(source, target): "swapped"|"moved"|"illegal"` (canSwap if target occupied, else canMove) — a transient plain-language flash reports the result; illegal drops are a no-op. The same `tryDrop` powers the picker's "Swap with · both stay valid" list (legalSwaps), so the swap path is identical whether dragged or clicked. DraggableLesson omits dnd-kit's `attributes` (no KeyboardSensor) to avoid nesting a role="button" inside the cell's own button. Ghost shows two ways: a faint "<Subject>?" hint in the empty grid cell and a highlighted "Suggested" first option in the picker (one click to accept). Live-verified end-to-end: swap applied (Mon P1 Maths ⇄ Fri P5 Sports, stayed "No clashes"), undo reverted, clear→ghost "Maths?"→accept restored "Full week". Bundle 70 KB gzip (dnd-kit +~14 KB), well under the 300 KB budget.

- 2026-06-07 (RB3) — Smart validation & fixes built as a thin PURE layer over the existing oracle, no new constraint logic. `domain/issues.buildIssues` maps validate()'s HARD violations to plain-language Issues (the messages already name entities, no codes — asserted by a "no /HE1|constraint/" test). There is NO separate "real clash vs joint event" logic: validate() already implements the event clash rule, so joint/team overlaps are never violations → `buildIssues(bundled) === []` (the test that proves "distinguishes real clashes from joint events"). The Issues panel is honestly framed as the safety net for clashes arriving from OUTSIDE the legal-only editor (teacher absence/reassignment RB8, availability edits, an 8p→6p profile switch dropping P7/P8, project import) — the editor itself can't create one, so RB3 is live-verified two ways: bundled shows no panel in the browser; the clash→issue→fix→undo path is a jsdom integration test that injects a clash into the store (`App.issues.test.tsx`).
- 2026-06-07 (RB3) — One helper, `issues.actionablePlacement(project,tt,violation)`, feeds BOTH click-to-jump and Fix-it so they can't diverge: it re-derives the placements at the violation's slot (the Violation only carries entity+slot, NOT which event to move) and returns the single movable one — not pinned, single-class normal (a shared joint/team event is never silently re-pointed). If every colliding placement is pinned/shared, it returns null and the issue is marked not-fixable (the AC's "fix where one exists"). `domain/fixes.suggestFixes` is orchestration over `legalOptions`/`canMove`/`clearCell`, ordered least-destructive: (1) reassign teacher (same subject, another qualified+available teacher), (2) move to the first legal free slot, (3) clear (monotonic fallback). Every candidate must STRICTLY reduce the hard count (afterHard < beforeHard) — a move that trades one clash for another is rejected. Fixes apply through `store.applyFix` (snapshot → undoable). Also fixed a latent bug: `store.reset` now restores `timetableId` (not just project), and added global RTL `cleanup` so a stale store-subscribed component can't re-render and crash when a later test mutates the shared store.

- 2026-06-07 (RB4) — Teacher load & insights as a PURE projection of deriveMaps (`domain/insights.ts`): teacherLoad/allTeacherLoads (used/available/free/perDay), loadBalance (min/max/avg/spread), and freeTeachers(day,slot). The free-teacher finder needs NO joint/team special-casing: deriveMaps already collapses a team_block / joint_class to ONE occupancy per teacher per slot, so "free" = schedulable ∧ available ∧ teacherCells has no occupancy at that slotKey — ELGA's 5 teachers and a senior-joint teacher are excluded for free. AC proven by an INDEPENDENT property oracle (`insights.test.ts`): per-teacher `used` recomputed straight from placements × occupiedSlots (no deriveMaps) equals teacherLoad for all 18 teachers; over all slots, freeTeachers ∩ occupied == ∅; plus explicit ELGA (Mon P3) / joint-English (Mon P4) / Mahesh-after-recess exclusions and Recess→[]. UI: a third "Insights" view tab (`ui/insights/InsightsView.tsx`) — a plain balance sentence, a teacher×day load heatmap (sky-tinted) with Week total + Free count, and a "Who's free?" finder. Live-verified via DOM (balance "10–47, avg 38.2 — uneven"; 18 rows; finder "1 free: Rakesh" at Mon P1); the preview screenshot tool timed out but the app is responsive (eval instant) — verification is by DOM + the property tests. Real loads are heavy (avg 38/48) — matches the analyses' "teacher availability is the binding constraint".

- 2026-06-07 (RB5 core) — Lock & auto-fill generator (`solver/fill.ts`, pure + seeded; `solver/rng.ts` mulberry32, NO Math.random per AGENTS §1). It NEVER moves/removes an existing placement — it only ADDS normal lessons into empty teaching holes — so pinned / joint_class / team_block are untouched by construction (HE8); asserted by a locked-signature test (every pinned/joint/team placement identical before/after). Search uses a fast INCREMENTAL occupancy index (teacher/class busy Sets + per-(class,subject) shortfall) encoding the legal-move rule, NOT deriveMaps per node — a DFS-backtracking prototype that re-derived per node blew the 5s budget (hung), and the index makes the inner loop ~100ms. Holes are ordered most-constrained-first (MRV by initial candidate count, seeded-jitter tie-break → per-seed variety, byte-identical per seed). DECISION: ship a GREEDY single pass (place the largest-shortfall legal candidate per hole), not full backtracking. Rationale: the AC is only "0 real clashes < 5s; pinned/joint/team never moved" — greedy meets it in ~62ms and fills ≥90% of cleared holes; the few holes whose sole qualified teacher is taken by a same-slot sibling class are left as visible gaps in the reviewable diff (the owner resolves them), never a silent clash. Full restoration via backtracking was prototyped but isn't worth its time/complexity for this instance; the occupancy index is the documented seam where it would slot in. validate() remains the single oracle — the AC test confirms the FINAL result has 0 hard. The worker wrapper + accept/reject diff UI are the next two RB5 increments (the AC test calls the pure solver directly in Node, never the worker).

- 2026-06-07 (RB5 complete) — Auto-fill wired through a real Web Worker (AGENTS §1: solver off the main thread). `solver/fillWorker.ts` is a thin marshalling shell (onmessage → pure fill() → postMessage); `solver/fillClient.runFill` runs it via `new Worker(new URL("./fillWorker.ts", import.meta.url), {type:"module"})` and FALLS BACK to a direct fill() when Worker is absent (Node/jsdom tests) — identical result since fill() is pure+deterministic. Vite emits a separate `fillWorker-*.js` chunk (verified in the build), confirming it's bundled as a worker. UI: a header "Fill the gaps" button → `FillReview` panel showing the FilledPlacement[] as a reviewable accept/reject DIFF (nothing silent): Accept applies via `store.applyFix` (undoable), Reject discards; both bump the seed so a re-run can try a different arrangement; any unfillable gaps are stated honestly. Live-verified end-to-end through the actual worker (clear Mon P1 → Fill → "Class 1 Mon P1 → Maths — Bindu" proposed → Accept → filled, 0 clashes, undoable) + a jsdom integration test (`App.fill.test.tsx`, accept + reject paths). RB5 COMPLETE.

- 2026-06-07 (RB6 engine) — Configurable rule engine `domain/rules.ts` (`evaluateRules`) compiles each enabled Rule (R1–R15) to a predicate over (project, timetable) → Violations in the shared shape; `must`→hard, `prefer`→soft. Wired into `validate()` (appended before the sort), so rule violations flow through the Issues panel, the badge, AND the move/swap/fix/solver legality gates for free. Bundled ships `rules: []`, so every prior test (bundled 0-issues, RB3/4/5) stays green. Plain-language messages name the offending entity/slot (no codes on the surface — test asserts no /R\d|HE\d/); `ruleSentence` (`domain/ruleText.ts`) gives the one canonical sentence used as the toggle label. Each of the 15 templates has a satisfied + violated test. **R16–R24 are doc-only (not in types.ts) — deferred.** **R7 is partial**: v6 `TimetableEvent` has no `allowedDays`/`fixedStart`, so R7 checks only "all placements of the block share one start slot" (placement-start-consistency), not "only on {days}" — recorded here per doc-first.
- 2026-06-07 (RB6 / legal-only ripple) — DECISION: configurable `must`-rules are NOT pre-excluded from the picker/ghost/solver; they surface as hard violations (fixable issues) and are respected by `canMove`/`canSwap`/`suggestFixes` via the hard-count gate. Rationale: aggregate rules (R10 spread, R12 weekly, R13 gaps) can't be judged per-candidate, and silently hiding cells for invisible reasons is worse UX than surfacing them. The task's explicit guarantee — the picker never offers an unqualified or clashing cell — still holds (legalOptions is unchanged). DATA_MODEL § legal-move rule updated in the same commit (doc-first): "legal" for the picker = structural invariants (qualification/availability/clash/duration); configurable preferences are surfaced, not pre-filtered.

- 2026-06-07 (RB6 complete) — `suggestRules` (`domain/suggestRules.ts`) proposes DESCRIPTIVE rules — patterns already true in the data: R7 for each team/joint block with a single start slot (ELGA), R12 teacher caps from observed daily/weekly maxima, R6 from observed double-period counts. Each is data-satisfying by construction, proven by a property test (every suggestion enabled alone → `evaluateRules` returns []), so a suggestion is never a fresh problem; deduped by scope. UI: a "Rules" view tab (`ui/panels/RulesPanel.tsx`) — sentence-first list of active rules (toggle on/off, remove) + "Suggested rules" with one-click Add (nothing silent, undoable via new `store.addRule/toggleRule/removeRule`). `prefer`/soft violations get a muted "Could be better" section in the Issues panel; `must` violations join "Things to fix". Header badge generalised "No clashes/N clashes" → "All clear/N to fix" (a rule violation isn't a "clash"); 3 UI tests updated. Live-verified: 19 suggestions on bundled, Add → "Your rules · 1", badge stays "All clear" (descriptive rule satisfied). RB6 COMPLETE (R1–R15; R16–R24 deferred).

- 2026-06-07 (RB7 complete) — Reports & export. `domain/exportLegacy.exportLegacyRawData` emits the OLD viewer's text format (DATA_MODEL § Legacy export): per-day `DayName` + `Class,Period 1..N` header + one row/class; cell = `Subject (Teacher / Teacher)`, teacherless study/free = `Subject`, empty = `Free`; joint/team events expand to the same cell for every member class (derive() does the expansion). Teaching slots map to Period 1..8 (Assembly/Recess excluded). Round-trip test reconciles every class/day/period cell against `deriveMaps` and asserts ELGA expands identically across the five primary classes. `domain/reports.ts` (workload via insights, per-class subject counts, clash report = HE1/HE2, school-wide free-cell count) — all projections of derive/validate, reconcile tests pass. UI: a "Reports" view = whole-school DAY sheet (the third school view) + teacher-workload table + "Download timetable file" (legacy text via Blob) + Print (browser print). AC: reports reconcile with derive() (tested); legacy export round-trips (tested); the three sheets' visual match to the school PDFs is the standard owner-side check. Live-verified (16-class day sheet, workload table, buttons). RB7 COMPLETE.

- 2026-06-07 (RB8 complete) — Operational tools. `domain/substitute.ts`: `coverOptions` = `insights.freeTeachers` (already excludes busy/unavailable/non-schedulable, incl. the absent teacher) ∩ qualified for EVERY (subject,class) the lesson spans — same legality bar as the editor; two-sided tested (qualified+free offered; busy/unavailable/unqualified excluded). `absentTeacherPlan` lists a teacher's lessons with covers; team_block (ELGA) flagged "arrange by hand" — v6 doesn't track which teacher↔which class inside a block, so auto-cover isn't representable (documented limit, same spirit as R7-partial). `domain/diffTimetables.diffProjects` compares two snapshots by RENDERED CELL VALUE (versions are deep copies — reference equality is meaningless); test: one edit → exactly one cell diffs. Store: `versions[]` + `saveVersion`/`restoreVersion` (restore is undoable — pushes to `past`)/`deleteVersion`; tested save→edit→restore→undo. UI: a "Tools" view (substitution planner with apply-cover for single-class lessons + named versions with restore + live "N cells differ" compare). PWA: pre-existing manifest + cache-first `sw.js` (relative paths) now REGISTERED in main.tsx — PROD-only (never touches dev/tests) and `BASE_URL`-aware (GitHub Pages subpath); dist ships manifest/sw/icons. Live-verified Tools view + versions; console clean. The numeric "installable offline" Lighthouse score is the standard owner-side check.
- 2026-06-07 (RB8 / scope honesty) — MOBILE TEACHER VIEW: the kickoff feature list names it but it's not an AC clause; the existing "By teacher" view is already responsive (overflow-auto, wraps) and readable on a phone, so no second view was built (noted, not silent). HEATWAVE PROFILE (DEFERRED, owner-visible): the kickoff names "the 6-period heatwave is a secondary switchable profile." The MECHANISM is ready — `buildHeatwaveProfile()` exists and switching profiles is a `Timetable.profileId` change — but a real switch needs a 6-period heatwave TIMETABLE (the bundled events are 8-period placements; pointing them at the 6-period grid would flood HE5/HE7). A 6-period transcription DOES exist in-repo — `fixtures/classWisePdf.ts` (M18, from `Class_Wise.pdf`) — but it PREDATES the 2026-27 source analyses (it is the pre-pivot "wrong skeleton" REBUILD.md explicitly moved away from), is a DIFFERENT schedule from the 2026-27 8-period grid (e.g. Class 1 Mon P2 = "EVS|Ravina" there vs "Sports|Rakesh" in the 2026-27 grid), and there is NO 2026-27 6-period source to reconcile it against. So no switch UI was added: shipping `classWisePdf.ts` as the current heatwave — even caveated — would risk presenting STALE data as the school's authoritative heatwave schedule, which the honesty rules forbid; and a half-working selector producing a clash-flood is worse than an honest gap. RESOLUTION: keep `classWisePdf.ts` as reference; defer the switch until the owner confirms a CURRENT 6-period heatwave schedule, then fold it via a heatwave variant of `buildProject` (period→slot map P1–P6 → slots [0,1,2,3,5,6], skipping the Break at index 4) + a second `buildBundledProject` + a profile selector. The mechanism (`buildHeatwaveProfile` + profileId switch) is ready the moment that data lands. This is the one kickoff bullet RB0–RB8 does not deliver, recorded here and in HANDOFF so it is not silent. (Supersedes the earlier "no transcription in hand" wording, which was inaccurate.)

- 2026-06-07 (post-RB8) — HEATWAVE DROPPED ENTIRELY (owner decision). The product is **8-period only**; the secondary 6-period "heatwave" switchable profile is removed, not merely deferred. Deleted: `buildHeatwaveProfile` + `HEATWAVE_PROFILE_ID` + `HEATWAVE_SLOTS` (profile.ts), the heatwave block in `profile.test.ts`, the dead 6-period reference fixture `fixtures/classWisePdf.ts`, and the second profile in `buildProject` (`profiles` now = `[regular]`). Comments referencing the heatwave (issues.ts, realGrid.ts, types.ts) updated. This supersedes REBUILD.md "Owner decision 1" (… heatwave 6p secondary) and DATA_MODEL's "profiles switchable (regular 8p / heatwave 6p)" — those plan lines are historical. 96 tests green (was 98; the 2 heatwave-profile tests removed), build + lint clean.

- 2026-06-07 (v6.1 CUSTOMIZE) — Phase to make the app fully editable + add an APPLIED constraint engine + fix Arts electives. Plan: docs/CUSTOMIZE.md (C1–C7). Static "rules" retired for a real `Constraint` entity that drives validate() (must) and generate()/score() (prefer) with live highlighting — a constraint that changes nothing is a bug.
- 2026-06-07 (v6.1) — Electives only in Arts 11 & 12; choose 3 of 4 (PolSci, Geography, Economics, EngLit), FREE choice (all 4 drop-combos occur). Commerce/Science subjects compulsory. Modeled via ElectiveGroup + StudentGroup; elective events scoped to student groups (events.studentGroupIds). No student group ever placed in a non-chosen subject.
- 2026-06-07 (v6.1) — Elective scheduling: Prakash teaches BOTH Economics & Geography → they can never run in the same slot. Under free 3-of-4 every elective pair is co-taken, so no two electives can be parallel without clashing someone → electives get distinct period-lines and the dropped subject becomes a supervised study period for that student group. Engine auto-parallelizes only when the actual chosen combinations are clash-free (fewer study periods if combos are later restricted).

- 2026-06-07 (C1) — Editable entities (full CRUD), safe-impact removal, and IndexedDB persistence. Built on TWO pure functions (`domain/references.ts`): `referencesOf(project,kind,id)` (the structured removal-impact preview the UI shows before any destructive edit — nothing silent) and `findDanglingRefs(project)` (scans every id-bearing field for orphans). The C1 invariant is `findDanglingRefs == []` after every CRUD op — asserted in `entityEdit.test.ts` alongside `validate() == 0 hard`. CRUD lives in `domain/entityEdit.ts` (pure, immutable, opaque collision-free new ids `t:n`/`s:n`/`c:n`, never name-derived so a later rename can't orphan them).
- 2026-06-07 (C1) — Rename is non-destructive by construction: it changes `name` only and keeps `id` stable. Ids ARE seeded from names in `buildProject` (id "Maths"), but nothing reads a name to resolve an id (eventSignature, quals, refs all key on id), so the id "Maths" with name "Mathematics" is fully consistent. Proven by a rename test that checks reference counts are identical before/after.
- 2026-06-07 (C1) — Removing a class/teacher is NOT a plain cascade for MULTI-CLASS events. Dropping a class from a 2-class joint_class (e.g. Economics = 11 Com+Arts) leaves 1 class, which violates HE6 (joint needs ≥2). So `removeClass` drops the class from each event's `classIds`, removes the event if it empties, and DEMOTES a joint_class/team_block that falls below 2 classes to a `normal` lesson. Tested on Class 11 Commerce (in both a 2-class Economics joint and a 3-class English/Hindi joint) and on a primary class in the ELGA team_block. `removeTeacher` optionally repoints all refs to a replacement (guided reassignment); without one, lessons go teacher-less (surfaced by the issues panel, never a silent clash).
- 2026-06-07 (C1) — Persistence: `src/persistence/db.ts` (idb), wired as a store WRITE-THROUGH subscription (`enablePersistence` in projectStore, called once from main.tsx) — persists on EVERY change incl. undo, so reload reflects the latest state. NOT debounced (saves are tiny; "survives reload" must hold even on immediate tab close). Load-if-present-else-bundled via `store.hydrate()`. **DB name is `timetable-studio-v6`, deliberately DIFFERENT from the legacy M19 DB (`timetable-studio`, store `projects`)** which still lingers in returning users' browsers — opening that existing v1 DB would skip our `upgrade` and our object store would never be created, so every write would silently fail. Caught by LIVE verification (the unit test uses empty fake-indexeddb and didn't surface it).
- 2026-06-07 (C1) — Schema growth handled by a FIELD-PRESENCE normalizer (`normalizeProject`) on load, not a version-gated migration ladder: it fills any missing array (incl. forward-compat seams `constraints`/`electiveGroups`/`studentGroups` that C3/C5 will populate), so a project saved by C1 loads cleanly under a later milestone. `schemaVersion` stays 6 across the arc. No Constraint/Elective types added to types.ts yet — those land in C3/C5 (advisor: don't add types you don't wire).
- 2026-06-07 (C1) — Period-grid editing scoped to rename/re-time + append-at-end + remove-with-impact. Mid-grid INSERT (which would reindex every `placement.slot` / `teacher.unavailable[].slot` / rule slot, with Recess pinned mid-array) is deliberately out of scope — not in the AC, high risk, low payoff. Removing a slot drops placements on it (shown as gaps) and clears teacher-unavailability for it; other slot indices are untouched (derive/validate look up by index, not position).

- 2026-06-07 (C2) — Assignments = the qualification matrix + class teacher. `domain/assign.ts` (pure): addQualification/removeQualification (idempotent; legalOptions iterates project.qualifications so an edit IMMEDIATELY changes what the picker offers — the AC test adds a fresh teacher+subject and asserts the option appears only after the triple exists), qualifiedTeachers, setClassTeacher (clears the field with undefined). Removing a qualification a placed lesson relies on is NOT auto-deleted — it surfaces as an HE3 issue (visible, fixable), never silently hidden.
- 2026-06-07 (C2) — "Class teacher takes period 1 daily" is wired through the EXISTING R4 rule (rules.ts already implements it, reading SchoolClass.classTeacherId, and is a no-op when unset). The UI's checkbox adds/removes an R4 rule with a deterministic id `R4:<classId>`, default severity `prefer` (soft — so enabling it surfaces as "could be better", not a hard clash) and is disabled until a class teacher is chosen. Tested: R4 yields 0 violations until setClassTeacher, then >0; clearing the class teacher returns it to 0 — proving "setting a class teacher enables the constraint." These R4 rules will migrate into the C3 Constraint model (carry forward, advisor checkpoint before C3).
- 2026-06-07 (C2) — UI: a new "Who teaches what" sub-tab in Setup (`ui/manage/AssignmentsView.tsx`) — class picker, class-teacher dropdown + P1 checkbox, and per-subject qualified-teacher chips (× to remove) with a "+ teacher" dropdown of the not-yet-qualified. All edits go through new undoable+persisted store actions (addQualification/removeQualification/setClassTeacher). Live-verified (set Bindu as Class 1 teacher, enabled P1, added Anita to Maths → "Bindu× Anita×"); console clean. 123 tests green.

- 2026-06-07 (C3) — Applied constraint engine. `Constraint` is a TYPED discriminated union on `template` (types.ts) — NOT the doc's earlier `params: Record<string,unknown>` sketch (updated DATA_MODEL doc-first). `scope`/`targetId` are UI hints; typed `params` are authoritative so a two-entity constraint ("Maths first half FOR Class 7") carries `subjectIds[]`+`classIds[]`. C3 ships three templates spanning the axes: `subject_half_of_day` (placement-local, two-entity), `teacher_max_per_week` (aggregate, teacher — owner example #2), `class_teacher_p1` (aggregate, class — migrated from C2's R4). Catalog breadth + suggester = C4.
- 2026-06-07 (C3) — Engine shape (advisor): a placement-LOCAL template defines ONE predicate `localViolates(constraint,profile,placement)`; `evaluateConstraints` filters every placed lesson through it (validate/highlight) AND `localMustForbids` applies the SAME predicate to a fill candidate — one oracle, can't drift. AGGREGATE templates define a whole-timetable evaluate only; fill does NOT pre-respect them (`localViolates` returns null) — they surface as issues and are the generator's job in C6 (the honest-gap pattern, like RB5 greedy / R7-partial). `evaluateConstraints` wired into `validate()` alongside the deprecated `evaluateRules`; must→hard, prefer→soft. fill's `candidatesAt` now skips candidates that break a local must (so "Maths first half for Class 7" is respected by Fill-the-gaps — the C3 AC). Gate test (`constraints.test.ts`): create the constraint → afternoon Maths flags the exact cell with a no-code message; fill never lands Maths in a Class 7 afternoon (non-vacuous: it still places morning Maths); disable → clears; per template satisfied=[]/violated≠[].
- 2026-06-07 (C3) — Migration: parallel-run (advisor) — keep `evaluateRules` (R1–R15) in validate() through C3 so RB6 tests + carried rules stay green; bundled ships `rules:[]` + `constraints:[]`. The ONE overlap risk is C2's R4 (class-teacher-P1): a C2-persisted project has an `R4:<classId>` rule, and C3's checkbox now creates a `class_teacher_p1` *constraint* — both would fire. Fixed by (a) AssignmentsView now creates the constraint (id `ctp1:<classId>`), and (b) a one-time load-time migration in `normalizeProject` (R4 rule → class_teacher_p1 constraint, drop the R4). Other R-rules have no C3 constraint equivalent yet, so they can't double-count until C4 ports them. Full R1–R15 port + `Rule`/`evaluateRules` removal + suggester = C4 (RB6 rule-suggester drops off the surface in C3, returns as the constraint-suggester in C4 — noted in HANDOFF, not silent).
- 2026-06-07 (C3) — Live cell highlighting: `WeekGrid` precomputes `Map<slotKey, messages[]>` from `validate()` ONCE (not per-cell — GridCell's per-cell deriveMaps is a pre-existing wart, not extended) and tints/outlines offending class cells rose with the plain message as a `title` tooltip. Class-scoped violations (half_of_day carries classId+day+slot) highlight in the class grid; teacher-scoped (max_per_week) live in the Issues panel. UI: the "Rules" tab is REPLACED by a "Constraints" tab (`ui/panels/ConstraintsPanel.tsx`, sentence-first builder + toggle/remove). `RulesPanel.tsx`/`suggestRules.ts` remain in-repo (unused on the surface) as C4 reference. Live-verified: Bindu max-5 must → badge "1 to fix" + "Bindu teaches 42 periods a week, more than the limit of 5"; disable → "All clear". 133 tests green.

- 2026-06-07 (C4) — Constraint catalog breadth: 24 templates implemented across ~6 evaluation shapes, math lifted from the deprecated rules.ts where an R-rule existed (R1→only_periods, R2→never_periods, R3→half_of_day, R9→board_protect, R10→spread, R11→subject_max_per_day, R12→teacher caps, R13→compact_day, R14→subject_order, R15→max_consecutive). Local (fill-respected) vs aggregate split preserved: locals (half_of_day, only/never_periods, not_last, teacher_not_first/last) route through `localViolates`; aggregates live in `constraintAggregates.ts`. Each has a satisfied([])+violated(≠[]) test on a controlled mini-project (`constraints.breadth.test.ts`, 25). Files split to respect the ~300-line rule: constraintShared.ts (helpers), constraintAggregates.ts (18 evaluators), constraintText.ts (sentences), constraintCatalog.ts (UI field descriptors), constraints.ts (dispatch).
- 2026-06-07 (C4) — DEFERRED catalog templates (model support absent / niche; not silent): `subject_needs_room` (no event.roomId is ever assigned, so it would flag everywhere), `subject_double`/`subject_min_gap_days` (need duration/gap modeling beyond current use), `specials_avoid_block` (no clean "near ELGA" semantics), `fair_first_last_duties`/`spread_activities` (broad global heuristics), `teacher_prefers_free_day`/`teacher_max_subject_per_day`/`class_start_with`/`class_end_with` (niche). The 24 shipped cover all four scopes + both severities + the local/aggregate split + both owner examples.
- 2026-06-07 (C4) — Data-driven builder: the Constraints panel renders from a descriptor table (`constraintCatalog.ts`: per-template field specs — subject(s)/class(es)/teacher/number/half/slots) through ONE generic form (`ConstraintsPanel.FieldInput`), so adding a template is a catalog row, not another inline branch (avoids a 600-line panel at 24 templates). Live-verified the form re-renders the right fields per template (subject_order→3 selects, teacher_max_per_day→teacher+number, class_board_protect→class+core-subject checkboxes).
- 2026-06-07 (C4) — "Suggest constraints" (`suggestConstraints.ts`): descriptive — proposes constraints ALREADY TRUE of the timetable (teacher weekly/daily observed maxima → caps; subjects confined to one half → half_of_day), so adding one never creates a fresh violation. Property-tested: each suggestion enabled alone → `evaluateConstraints` returns [] (`suggestConstraints.test.ts`); deduped by (template, params); ≥5 on bundled (19 produced). Mirrors the retired RB6 rule-suggester pattern. Live-verified: 12 shown, "Add" lands one in Your constraints, badge stays "All clear".
- 2026-06-07 (C4) — RULES RETIRED (the planned C4 final step, on a green base). Deleted rules.ts/.test, ruleText.ts, suggestRules.ts/.test, ui/panels/RulesPanel.tsx; removed `Rule`/`R1..R15Rule`/`RuleSeverity`/`RuleBase`/`SlotRef`/`RuleTemplate` and `Project.rules` from types.ts (kept `HalfOfDay`, used by constraints); dropped `evaluateRules` from validate(); removed store add/toggle/removeRule; entityEdit now PRUNES constraints referencing a removed entity (was rules) — `pruneConstraints` drops single-id-param constraints and empties-out array-param ones, keeping `findDanglingRefs` (extended to scan constraint params) at [] after CRUD. `normalizeProject` migrates a persisted R4 → class_teacher_p1 off the raw blob and never carries `rules` forward (tested). 143 tests green (was 160; −18 deleted rule tests +1 migration test); lint + build clean (~89 KB gzip, smaller).

- 2026-06-07 (C5) — Electives & student groups (the Arts fix). KEY INSIGHT: the bundled grid already runs the four Arts electives in distinct per-class slots (HE2 forces it), so this is a MODELLING fix, not a re-schedule. `seedArtsElectives` (domain/electives.ts, pure, idempotent) defines, per Arts class, an ElectiveGroup {PolSci,Geo,Eco,EngLit; choose 3} + four free-3-of-4 StudentGroups (one per dropped subject), scopes each elective EVENT to the groups that chose it (`studentGroupIds`), and adds a Self Study event for the dropping group at exactly that elective's slots. Applied in buildBundledProject; `buildBundledProjectRaw` (no electives) backs the cell-for-cell round-trip test (the elective model intentionally diverges from the source's forced-sitting representation).
- 2026-06-07 (C5) — THE clash-rule refinement (advisor, load-bearing): class-clash (HE2) now compares ATTENDEE SETS, not just distinct eventIds. `attendeesOf(event, classId, groupsByClass)` returns whole-class as a UNIVERSAL set (NOT empty — empty∩empty would falsely clear HE2 for every non-elective class) when the class has no groups or the event lists none of this class's groups (so a joint Economics reads Commerce=all, Arts=3 groups correctly); else the listed groups. Two events in a (class,slot) clash iff their attendee sets intersect → elective(3 groups)+Study(1 dropper) is legal (disjoint), two whole-class lessons still clash, an elective vs a whole-class lesson clashes. Teacher clash (HE1) unchanged, so Prakash in Eco+Geo same slot is still caught (it's HE1, not a feature to build — a test to write). Generalizes the joint/team "same-event overlap is legal" to "disjoint-audience overlap is legal".
- 2026-06-07 (C5) — GATE met: buildBundledProject() → validate() == 0 hard (the Study events do NOT trip the refined HE2 — the thing that would silently break). No student group is ever in a non-chosen subject (asserted: every elective event's studentGroupIds ⊆ choosers). Two-sided clash test: legit elective+study overlap → no clash; an injected same-group double-book → clash. `canParallelize(groups,a,b)` = no group chose both → false for every pair under free-3-of-4 (→ distinct lines + study fallback, the bundled state); real parallel re-scheduling is C6. Per-student personal timetable is C7 (not built now).
- 2026-06-07 (C5) — Consequences fixed honestly: (a) `reports.subjectCountReport` now counts every DISTINCT event per slot (an option line holds 2+), so per-subject sums reconcile with the occupancy recompute (was counting only occ[0]); (b) `fill.test` clears only non-group-scoped normals — clearing an elective leaves its Study shadow which legitimately blocks a whole-class refill (attendee-aware elective refill is C6), so it would skew the "fill ordinary gaps ≥90%" quality check. (c) WeekGrid renders option-line slots stacked ("Option line" + each elective/Study with its chosen-group note). Returning users: `normalizeProject` runs the idempotent `seedArtsElectives` on load, so a pre-C5 persisted VPPS project migrates to the elective model (tested). 153 tests green; lint+build clean (~90 KB gzip). Live-verified: Class 11 Arts shows option lines (EngLit for 3 groups + Self Study for the drop-EngLit group), "All clear".

- 2026-06-07 (C6, part 1/2 — generator honours every pre-respectable must) — `solver/caps.ts` (`buildCapGuard`): the generator now refuses any candidate that would breach an enabled MUST cap, using running counters SEEDED from the kept placements, so a regenerate cannot produce a hard cap violation it claims to honour (one-oracle with validate). Scope decided WITH the advisor (5-vs-all was the wrong axis): pre-respect EVERY monotone counter cap — `teacher_max_per_week/per_day/days_per_week`, `teacher_min_free_per_week` (the SAME cap as `lessons ≤ days·teach − min`), `subject_max_per_day` (subject-SET × class-SET membership), `class_max_teachers_per_day` (increments only for a teacher NEW to that class+day), the run-based `teacher_max_consecutive` / `class_max_consecutive_same` (run computed THROUGH the candidate over teach-index adjacency, never raw slot numbers — Recess sits mid-array — so a pre-existing over-cap run in kept placements never blocks an unrelated candidate), plus two cheap local musts (`class_board_protect`, `subject_not_adjacent_to`). The documented boundary: genuinely NON-monotone musts (`subject_order`/`subject_spread_min_days`/`teacher_compact_day`/`class_daily_variety`/`balance_teacher_loads`/`core_subjects_early`) cannot be greedily pre-respected — they are surfaced by validate() and reduced by the multi-seed pass (part 2). `min_free` was re-bucketed from non-monotone to a cap on the advisor's correction.
- 2026-06-07 (C6, part 1/2 — blocker report) — `FillResult` gains `blockers: string[]`: a post-pass diagnosis (one per still-short (class,subject), not threaded through the hot loop) explains WHY in plain language — no qualified teacher / a binding cap (probed at every slot where the TEACHER is free, since a weekly cap blocks regardless of slot, so the binding cap surfaces even when the class's own holes fall where the teacher teaches elsewhere) / class week full / all qualified teachers booked / unresolvable clash. Surfaced in `FillReview` as a bulleted "Why some lessons couldn't be placed". Canonical infeasible test: pick the teacher with the largest FORCED load (sole schedulable qualifier for non-elective (class,subject) reqs — electives excluded as their option-line/self-study shadows read as "class full" not "teacher capped"), clear their lessons, cap one below → blocker names teacher+cap, validate shows 0 hard for the cap, gaps remain. One-oracle proven generically: for every pre-respected template, a tight must → fill introduces 0 new hard violations of it (`caps.test.ts`, 13 tests). Electives-not-broken: regenerate Class 11 Arts → 0 hard, no whole-class lesson lands in an option-line slot. 166 tests green; lint+build clean (~92 KB gzip).
- 2026-06-07 (C6, part 2/2 — prefer optimisation by best-of-N) — `solver/generate.ts` (`generate`): since fill() now pre-respects every hard must it can (part 1), EVERY seed it produces is legal — what a single greedy pass can't do is optimise the prefers it can't pre-respect (spread/order/compact/variety/balance). generate() runs a FIXED seed list (1..N, default 8), scores each fill by validate() SOFT count, and keeps the lowest (ties → fewer gaps → lowest seed). Deterministic, no Math.random (AGENTS §9). Wired through the worker (`fillWorker` runs generate when `seeds>1`, else single fill; `fillClient.runGenerate` with main-thread fallback) and the "Fill the gaps" button now calls `runGenerate` (the old per-click seed re-roll is gone — best-of-N is deterministic). `GenerateResult extends FillResult` (+seed/softScore/triedSeeds), so FillReview is unchanged. Tests (`generate.test.ts`, 3): legal (0 hard) + reports triedSeeds; PICKS THE MINIMUM soft across seeds (not merely seed 1, with a prefer constraint that varies by arrangement); deterministic. 169 tests green; lint+build clean (~92 KB gzip main, worker chunk grew to ~28 KB raw as it now bundles the constraint engine — fine, off the main thread). Live-verified: app loads, "Fill the gaps" runs through the real Worker (complete grid → "already complete"), console clean. C6 AC fully met.

- 2026-06-07 (C7 — reports, student view & polish) — `domain/studentView.ts`: `studentTimetable(project,timetable,studentGroupId)` returns the events a chosen combination ACTUALLY attends (every whole-class lesson + the one elective/Self-Study scoped to that group), built on the SAME oracle as HE2 (`deriveMaps` + `attendeesOf`) so a personal timetable can never disagree with the grid. Relies on the slot-exclusive invariant (on a clash-free grid a student attends EXACTLY ONE event per slot — option lines are slot-exclusive per HE2, every dropper has a Self-Study shadow); the C7 test verifies this INDEPENDENTLY (it recomputes per slot the count of distinct events whose `attendeesOf` includes the group and asserts ==1 — NOT via `personal.size`, which `studentTimetable`'s break already collapses and so could mask a double-attend). On a NON-clash-free grid (two whole-class lessons double-booked) the personal view shows one of the clashing lessons — that is not hidden; the clash is surfaced by validate()/the Issues panel, which is where a double-booking belongs. We deliberately do NOT throw (it would crash report rendering on exactly the dirty grid being debugged). `electiveReport(project)` = the human-readable option-line "who's in what". AC test (`studentView.test.ts`, 10): for every Arts combination the elective subjects in the personal view equal `electiveSubjectIds` EXACTLY (3 chosen present, dropped ABSENT, Self Study present, compulsory present), and reconcile-with-derive (each busy class slot → exactly one attended event matching the grid).
- 2026-06-07 (C7) — UI: `ReportsView` gains a per-student `PersonalTimetable` (class + combination picker → personal grid), an `SchoolReports` block (elective/option-line report, per-class subject counts surfacing the existing `subjectCountReport`, room use), and the whole-school day sheet now renders OPTION LINES (every distinct event in a cell, not just occ[0]) so it matches the grid. Split into three files to respect the ~300-line rule. Print polish: controls `print:hidden`, the day-selector replaced by a static "· {day}" label in print, sections `breakInside: avoid`. Pixel-exact match to the school's PDFs stays owner-side (carried claim).
- 2026-06-07 (C7) — Room-use is HONEST, not faked: `roomUseReport` counts distinct events naming each room, but no lesson sets the optional `event.roomId` yet (rooms:[] in the data) — the SAME gap as the deferred `subject_needs_room` constraint (C4). So the report shows real utilisation IF rooms are ever assigned, else an owner-visible note ("Lessons aren't assigned to rooms in this timetable, so room utilisation can't be reported yet") rather than a misleading empty table. Legacy text export retained (spec). 180 tests green; lint+build clean (~93 KB gzip). Live-verified: the "PolSci/Geography/EngLit" (drop-Economics) combination shows those three + compulsory + Self Study, no Economics; all six report sections render; console clean. **v6.1 CUSTOMIZE (C1–C7) COMPLETE.**

- 2026-06-19 (OVERHAUL A1 — Self Study collapse fix) — The reported "re-plan fills Self Study everywhere" bug: `planTimetable`'s `planningBase` stripped every unpinned normal/1-class/duration-1 placement, which INCLUDED the four Arts electives (they ARE normal/1-class/duration-1 but carry `studentGroupIds`) while KEEPING the paired group-scoped Self Study (type `self_study`, never removable). The electives' slots stayed occupied by the kept Self Study, so fill (correctly marking the whole Arts class busy there) could never rebuild them and the class collapsed into whole-class Self Study. FIX (minimal, correct): `canPlannerRemove` now also refuses any event with a non-empty `studentGroupIds` (an elective) — electives + their Self Study are now immovable like joint/team blocks, so the planner re-schedules only ordinary whole-class lessons. DELIBERATELY did NOT make fill's `classBusy` attendee-aware (the plan floated it): fill only ever places WHOLE-CLASS lessons, which genuinely cannot share an elective slot (whole-class ∩ any group intersects → HE2 clash), so marking the whole class busy at elective/Self-Study slots is the correct conservative behaviour; making it group-aware would let fill drop a whole-class lesson into an option line and REINTRODUCE clashes. Repro test added FIRST to `plan.test.ts` (re-plan bundled → all four electives survive for Arts 11 & 12, Self Study count does not grow, 0 hard). 190 tests green.

- 2026-06-19 (OVERHAUL A2 — coverage is a first-class, separate fact) — New `domain/coverage.ts` answers "is ENOUGH placed?" (does each class get its required periodsPerWeek per subject), kept SEPARATE from validate() which answers "is what's placed LEGAL?" (clashes/qual/availability). Rationale: they are different questions and the architecture already separates validation from completeness; overloading HE-codes with coverage would muddy the clash oracle and risk false hards when requirements/grid drift. Counting MIRRORS buildProject's reqCount and fill's placedCount (one tally per occupied teaching slot per class; duration-2 counts twice; team_block once per class per slot) so the three never disagree. `requirementCoverage`/`coverageGaps`/`totalShortfall`. Tests: bundled → 0 gaps (reconciles), removing a lesson → exact shortfall, electives covered per class. The UI status becomes three-state (Clashes / Incomplete / Ready) in Phase C — no more green "All clear" over a half-empty grid.
- 2026-06-19 (OVERHAUL A3 — complete-as-feasible solver) — Per the advisor: do NOT build systematic DFS or add caps rollback. The cap guard is a greedy-loop optimisation, not the oracle — every cap-must is ALREADY a hard violation in validate(). So the new repair layer (`solver/schedule.ts`) is a bounded, seeded, VALIDATE-GATED local search: keep `fill` as the MRV+forward-checking constructor, then for each remaining coverage gap try a direct placement or a single-eviction-relocation (move ONE ordinary lesson out of the way), accepting a candidate iff validate() shows 0 hard AND totalShortfall strictly drops. Strictly-improving acceptance ⇒ can never regress a valid timetable (keeps all prior tests green by construction); caps enforced for free via the gate. `isReschedulable` (shared with plan.ts) is the movable set — excludes pinned/joint/team/electives+self_study (so the repair can't resurrect the A1 Self Study bug). `generate` runs `solve` per seed under a shared wall-clock budget (per-seed slice), ranks by COMPLETENESS first then soft (completeness is the owner's priority), early-exits at 0/0.
- 2026-06-19 (OVERHAUL A3 — "Make best timetable" stops nuking the board) — The big real-world fix: `planningBase` used to strip EVERY reschedulable lesson then rebuild from scratch, which discarded a complete valid timetable and (greedy+repair) could only recover ~98% (live measure: from a fully-cleared bundled, 456 demand → greedy 15 short → repair 13 → best-of-12 10). Now it strips ONLY the ordinary lessons that are actually in a HARD violation (mapped from validate()'s flagged class/teacher/event cells), keeping every valid placement; on an already-valid board nothing is stripped and the timetable returns INTACT and complete. This satisfies all three existing plan.test cases (half-of-day must → the violating cell is stripped & re-placed in the first half; pinned → not stripped, blocker "locked"; no-qualified → the unqualified placement is in an HE3 hard, stripped, can't be re-placed, blocker "qualified"). Boundary (honest): a from-SCRATCH full-school build still leaves ~10 gaps (deep multi-way contention beyond depth-1 eviction) — surfaced as honest blockers/Incomplete, never a silent clash; this is the documented limit of greedy+single-eviction+best-of-N (the approved plan's verification explicitly allows "honest blockers if genuinely infeasible"). Tests: `schedule.test.ts` (contended instance completes 0/0 across seeds; repair ≤ greedy on cleared bundled; solve never regresses bundled; planTimetable returns bundled complete+clash-free). 197 tests green; build + lint clean (~98 KB gzip main; worker ~39 KB).

- 2026-06-19 (OVERHAUL B — configurability) — `domain/requirementsEdit.ts` (`setRequirement` upsert / `removeRequirement`, pure + tested): the store's `setRequirementPeriods` is now an UPSERT (creates the (class,subject) quota if missing, seeding teacherIds from qualifications) and a new `removeRequirement` action drops one. Entity-removal pruning was already handled by entityEdit.ts. This is the data spine for the new "Weekly subjects" (compulsory-per-week) editor — the thing the owner could not state before.
- 2026-06-19 (OVERHAUL C — green-field UI) — The React layer (`src/ui`) was rebuilt around a calm guided shell while REUSING every tested domain/solver core and the proven interaction components (WeekGrid/TeacherGrid/DayGrid, CellInspector, IssuesPanel, ManageView, AssignmentsView, ConstraintsPanel, Reports/Tools). New: `ui/app/App.tsx` (sidebar + topbar + section router), `Dashboard` (three-state Ready/Incomplete/Clashes status from `ui/app/status.ts` = validate + coverage, big CTA, guided 4-step checklist, stat cards), `GenerateView` (one clear Make-timetable/review screen surfacing coverage gaps + blockers + the change list), `TimetableWorkspace` (the editor), `setup/SetupHub` (People & subjects · Weekly subjects · Rules) and `setup/QuotasView` (the new quotas editor with live per-subject placed/short badges). Design tokens added to index.css (.ts-card/.ts-btn-*/.ts-input). The 5 App-level tests + 3 component DOM tests were updated to the new IA (nav labels, the grid combobox gained aria-labels). NOTE: the sidebar "Generate" route was renamed from "Make timetable" to avoid colliding with the action button's accessible name. 201 tests green; build (~101 KB gzip main) + lint clean. Live-verified in Chrome: dashboard, timetable grid, Generate (re-plan of the complete bundled = 0 changes / 0 missing / 0 clashes — completeness preserved), and the Quotas editor all render and work.

- 2026-06-19 (OVERHAUL — recovery: repair-a-class + persisted restore points) — The owner's Class 12 Arts had been corrupted by the OLD Self Study bug and persisted to IndexedDB (the fix prevents NEW damage but can't un-persist old). Two recovery features: (1) `domain/restore.ts` `restoreClassFromReference(project, reference, classId, ttId)` (pure) rebuilds ONLY a class's own single-class lessons (electives, Self Study, ordinary subjects) from a reference (the bundled original), leaving shared joint/team events and other classes untouched — residual clashes with since-moved shared lessons are left to validate() rather than papered over. Note: of the four Arts electives, Geography/Political Science/English Literature are single-class (what the bug stripped) while Economics is a joint Arts+Commerce class (never stripped), so per-class restore correctly targets the former. Store `restoreClass` (undoable), Tools "Repair a class" card (defaults to Class 12 Arts). (2) Persisted RESTORE POINTS: `persistence/db.ts` gains `saveBackups`/`loadBackups` (same object store, new key "backups", cap 15) so they survive reload; store `backups`/`createBackup`/`restoreBackup`/`deleteBackup`, hydrated on load; App auto-creates a restore point BEFORE applying any plan; Tools "Restore points" card to roll the whole timetable back. This is the durable "undo for these kinds of bugs" the owner asked for (the in-memory undo doesn't survive reload). Tests: `restore.test.ts` (damaged Class 12 Arts → restored, Economics untouched, other classes untouched, 0 hard). 203 tests green; build (~102 KB gzip) + lint clean. Live-verified in Chrome (repair runs, confirmation + Undo; restore-points panel renders).

- 2026-06-20 (OWNER decision — drop the elective option-line / Self Study model) — The owner's authoritative timetable (the legacy timetable2025 viewer) shows Arts electives as ordinary whole-class lessons with NO per-student "Self Study" splitting. The C5/C7 option-line model (group-scoped electives + a supervised Self Study for droppers) was making Class 11/12 Arts read as full of "Self Study", which the owner reported as broken. DECISION: the DEFAULT product no longer uses the model. `buildBundledProject` now returns the raw grid (`buildBundledProjectRaw`) — electives are plain single-class lessons at their distinct slots, exactly like the legacy. New pure `stripElectiveModel` (domain/electives.ts) reverses the seeding (drops `evt:study:*` Self Study events + placements, clears `studentGroupIds`, empties elective/student groups) and `normalizeProject` ALWAYS applies it on load, so returning users (incl. the owner) are auto-migrated to plain electives. The model code (attendees.ts HE2 attendee-sets, seedArtsElectives, studentView) is KEPT but DORMANT (not applied by default) so it isn't lost and stays tested — the C5/C7 tests now seed electives EXPLICITLY (`seedArtsElectives(buildBundledProject())`). Note: of the four Arts electives, Geography/Political Science/English Literature are single-class (restorable per-class) while Economics is a joint Arts+Commerce class. Combined with `restoreClass`, the owner recovers a corrupted Class 12 Arts by: load (auto-strips Self Study) → Tools → Repair a class → Class 12 Arts (restores the plain electives). db.test now asserts the strip migration; 203 tests green; build (~101 KB gzip) + lint clean. Live-verified: Class 12 Arts shows Geography/Political Science/Economics/English Literature, no Self Study, no Free.

- 2026-06-21 (M24 — Feasibility & relaxation engine) — Added `RelaxationSuggestion` and `Blocker` to `src/solver/types.ts`; `FeasibilityReport` gains optional `structuredBlockers?: Blocker[]` (backward compatible — no existing construction site needs updating). The 6 checks: `subject_capacity` = no qualified teacher for a requirement; `class_capacity` = class demand > weekly slots; `teacher_capacity` = teacher's `Teacher.maxPerWeek` field < forced demand (requirements where they are sole qualifier); `slot_contention` = `subject_only_periods` must constraint reduces available slots below weekly demand; `locked_conflict` = pinned event violates a hard constraint; `cap_sum` = `teacher_max_per_week` must constraint params.max < forced demand. `teacher_capacity` and `cap_sum` are separate concerns: one checks the intrinsic field (always present), the other checks an explicit user constraint (only when one exists). Joint-class deduplication: when computing forced demand, `joint_class` events (teacher simultaneously teaches multiple classes) are counted ONCE via event inspection — without this, a teacher sharing a single 4-period joint event across 3 senior sections would appear to need 12 teaching slots instead of 4, falsely blocking the bundled VPPS project. `apply` (teacher_capacity: set Teacher.maxPerWeek = forced; cap_sum: set constraint.params.max = forced) is a pure, round-trip-verified relaxation. slot_contention is implemented as periods-based (subject_only_periods constraint × profile.days count) — note: day-based restrictions ("Mon–Thu only") cannot be expressed in v6 (no allowedDays field); this is the tractable scoped interpretation. AC met: 228 tests green (17 new); build + lint clean; apply round-trips verified.

- 2026-06-20 (M23 — Rules/Preferences vocabulary) — `tierLabel(severity): "Rule" | "Preference"` added to `domain/constraintText.ts` (re-exported from `domain/constraints.ts`) is the SINGLE source of truth for the vocabulary. Why a named function rather than inline ternaries: if the vocabulary ever changes (e.g. "Hard Rule") one edit propagates everywhere. No schema change — `severity: "must"|"prefer"` stays in storage; this is purely presentational. Two-section ConstraintsPanel: the form always shows; "Add Rule" and "Add Preference" buttons replace the single "Add constraint" + severity dropdown — pre-setting severity at the click, never requiring a separate dropdown. Moving between sections uses `onAdd(constraint, newSeverity)` (the same callback handles creates and updates via `addOrUpdateConstraint` in App.tsx which checks id existence). The dashboard change from one "Rules" tile to separate "Rules"/"Preferences" tiles required a grid expansion from 4 to 5 columns — `sm:grid-cols-5`. Issues.tsx "Things to fix" → "Rule broken" to match the tier vocabulary. AC met: 214 tests green; build + lint clean; live-verified two-section panel + "Rules · 0" / "Preferences · 0" sections with no raw codes on the surface.

- 2026-06-21 (M25 — Assessment engine) — New `src/domain/assessment.ts` (`assessTimetable(project, timetableId, opts?): Assessment`). Types co-located in `assessment.ts`, not `domain/types.ts`: Assessment is computed output (like CellDiff in diffTimetables.ts), not persisted schema. Five dimensions: coverage (delegates to coverageGaps), teacherFairness (delegates to allTeacherLoads+loadBalance), pedagogy (soft violations from validate()), boardProtection (heuristic: academic subjects in last-quarter teaching slots for isBoardClass classes, no constraint required), stability (delegates to diffProjects when opts.baseline given). opts.baseline typed as Timetable (per plan spec) — a synthetic 'before' project is constructed for diffProjects, which reads activeTimetableId; stability dimension is omitted entirely when no baseline is passed. Score = 100 − penalties (8/gap-period, 5/hard, 1/soft, 2/board-late-academic); clamped [0,100]; band thresholds Great≥80/Good≥60/Fair≥40/Poor<40. Property test verifies teacher fairness numbers against deriveMaps cell counts directly (not via allTeacherLoads, which assessment.ts already calls). Messages never contain constraint codes. 19 new tests (assessment.test.ts); 247 total; build + lint clean.

- 2026-06-21 (M26 — Multi-candidate generation) — New `src/solver/presets.ts` (Preset interface + 3 presets as data). Presets are pure data (label + multiplier map over ConstraintTemplate) — no logic. New `generateCandidates()` added to `generate.ts` alongside the existing `generate()` which is unchanged (all callers — deepSearch, plan, fillWorker — unaffected). Key design choices: (1) Non-overlapping seed ranges per preset (preset 0 → seeds 1-N, preset 1 → seeds N+1-2N, …): without this, all presets converge on the same best seed (they all try seeds 1-N) and the dedup hash collapses them to one candidate. Different seed ranges give each preset genuinely different random orderings. (2) Weighted soft scoring: Σ(constraint.weight × multiplier[template]) per soft violation. The multiplier map makes different presets select different seeds as "best" — a teacher-friendly preset (balance_teacher_loads × 3) selects the seed with best teacher balance, while student-focused (core_subjects_early × 3) selects differently. (3) Dedup by placement hash: if two presets select the same seed range AND reach the same greedy result, only one candidate is returned. (4) Candidate type added to solver/types.ts (imports Assessment from domain/assessment.ts — layering holds: domain ← solver). Verdict derives from shortfall + hardCount; impossible/timeout reserved for deep-search path (M27/M28). (5) assessTimetable called once per surviving candidate (after dedup) to avoid redundant computation. 8 new tests; 255 total; build + lint clean.
