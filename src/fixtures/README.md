# Fixtures

- `vpps.sample.ttproj.json` — built in M1 by running `legacyImport` on a snapshot
  of the real viewer `rawData`. Used by solver/validation tests.
- Small synthetic projects for unit tests live alongside the tests that use them
  (clearly marked as fixtures, never invented school entities — AGENTS.md §2).
